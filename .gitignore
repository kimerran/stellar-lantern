node_modules
dist
dist-mobile
# Generated activity reports contain real user activity — never committed (#81).
dist-report
*.local
.DS_Store
.vite

# Internal specs, brand, and implementation plans
SPEC.md
BRAND.md
*-spec.md
*-plan.md

# Packaged extension artifacts
release

# Capacitor / Android build outputs
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
android/capacitor-cordova-android-plugins/

# Internal client work
instawards/

# Soroban contract build output
contracts/blacklist-registry/target/

# Tester label maps for `report:activity --map` hold install IDs — never committed (#98).
reports/
