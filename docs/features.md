# Features log

A running, reverse-chronological log of shipped changes. The auto-dev agent
appends an entry here for every change it lands (see `auto-dev.md`).

Format: `- YYYY-MM-DD — <summary> (#PR, closes #issue)`

## Shipped

- 2026-07-12 — a11y fix: **Form input errors are now announced to screen readers** (`popup/components/Input.tsx`, part of #121 — UI/UX review, slice 3). The shared `Input`'s `error` prop rendered as a plain sibling `<span>` with no programmatic link to the field — no `aria-invalid`, no `aria-describedby`, and the message wasn't a live region, so screen-reader users got no association or announcement on validation failure. Now `Input` sets `aria-invalid` when errored, links the message via `aria-describedby` (merging any caller-provided value, keyed off `useId`), and marks the error `role="alert"` so it's announced. Fixes the error path for the 9 screens that use `Input` (Send, Guardians, RecoverAccount, SmartAccount, Onboarding, Settings, Earn, CoSignRecovery, Unlock). Additive, no visual change; typecheck + lint + full suite + build green. Remaining #121 items (`aria-current` on bottom nav, secondary-control touch targets, amount contrast, icon-font bundling) left open on the issue. (part of #121)
- 2026-07-12 — a11y fix: **Consistent keyboard focus ring on the popup's interactive controls** (`popup/components/Button.tsx`, `popup/components/BottomNav.tsx`, `popup/screens/Assets.tsx`, `popup/screens/Earn.tsx`, part of #121 — UI/UX review, slice 2; **stacked on slice 1** #122). Form inputs and the auto-lock `<select>` already had a deliberate amber focus treatment, but the primary action controls had none and fell back to the browser default outline against the dark theme (WCAG 2.4.7). Added a shared `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-container` to the `Button` base (covers primary/secondary/ghost app-wide), the bottom-nav tabs, the Home quick-action tiles, and the Earn Supply/Withdraw buttons — so keyboard/screen-reader users who sign transactions always see where focus is. Uses `focus-visible` (not `focus`) so mouse clicks don't show the ring. Pure styling; typecheck + lint + full suite + build green. Remaining #121 items (`aria-current` on bottom nav, Input error aria wiring, secondary-control touch targets, amount contrast, icon-font bundling) left open on the issue. (part of #121)
- 2026-07-12 — UX/a11y fix: **Earn pool action buttons no longer clip "Withdraw" at the 360px popup width** (`popup/screens/Earn.tsx`, part of #121 — UI/UX review, slice 1). Each reserve row was a horizontal `flex` with a fixed `w-24` info column + a `grid-cols-2` button area, leaving ~92px per button — too narrow for the icon + "Withdraw" + padding, so the label was cut off at the card's right edge (visible in `docs/ui/overview.png`). Restructured the row to stack the code/APY info **above** a full-width two-button grid, so each button gets ~half the card width and can't clip. Also gave `ActionButton` a `min-h-[44px]` so the Supply/Withdraw targets meet the 44px touch minimum (were ~34px). Pure layout change, no behavior touched; typecheck + lint + full suite green. Remaining #121 items (focus-visible ring, `aria-current`, Input error wiring, secondary touch targets, amount contrast, icon-font bundling) left open on the issue. (part of #121)
- 2026-07-12 — UX: **Finalized bottom nav (Home · Apps · Activity · Settings) + Activity/Settings as inline tabs** (`popup/components/BottomNav.tsx`, `popup/App.tsx`, `popup/screens/{Activity,Settings}.tsx`, `popup/components/AppBar.tsx`, part of #110 — slice 3, **stacked on slice 2** #113; completes the epic). The last piece of the Phantom-style overhaul: the bottom nav moves from `Assets · Earn · Send · Apps` to the target IA **`Home · Apps · Activity · Settings`** (amber active-pill kept). Earn and Send are no longer nav tabs — they're the Home quick-actions from slice 2 (still ≤1 tap) — so their tab values persist as **Home sub-views** (the nav keeps **Home highlighted** while Send/Earn are open, via `active={tab==='send'||tab==='earn' ? 'assets' : tab}`). **Activity and Settings became inline tabs** rather than overlays: both gained an `embedded` variant (mirroring Earn's `embedded` pattern from #91) that drops the full-screen chrome/back-header and renders inside the tab's scroll area under the app bar — `Activity` shares one `body` across the overlay + embedded paths (the `TxDetail` drill-in still works inline), `Settings` shares one `content`. The Send-success flow now lands on the **Activity tab** (`onDone → setTab('activity')`) instead of an overlay. With Settings now a first-class nav tab, the **app-bar gear was removed** — the top bar is down to just **identity + display-only network badge** (+ the popup "open in full tab" affordance) — and the redundant **Activity row was dropped from the Settings hub** (it's a nav destination now; `onOpenActivity` plumbing removed). The overlays reached *from* Settings (Receive, Guardians, scam-check, Cash) still open full-screen on top of the tab and return to it. **No capability lost, security flows untouched** — Guardians/Cash/Scan still open the same screens with the same scan→review→sign gating; the testnet/mainnet badge distinction is unchanged. typecheck + lint + full suite (419) + `build`/`build:mobile` green (screens verified via build — repo has no component-test infra). **Completes #110's acceptance criteria** (Settings hub #111 → Home quick-actions #113 → this nav-finalize slice). (closes #110)
- 2026-07-12 — UX: **Home is action-forward — Send · Receive · Swap · Earn quick actions** (`popup/screens/Assets.tsx`, `popup/App.tsx`, part of #110 — slice 2, **stacked on slice 1** #111). Continues the Phantom-style overhaul: the Home (Assets) screen now leads with a prominent **4-up quick-action row** — **Send · Receive · Swap · Earn** — each an amber-tinted icon tile reachable in **a single tap**, sitting directly under the balance hero. This replaces the old cramped Send/Swap/**Refresh** button row: Send and Swap keep their targets (Send tab / Swap overlay), **Receive** (opens the QR overlay) and **Earn** (switches to the Earn tab) are promoted from being buried in the app-bar icon row / bottom nav into first-class home actions, and **Refresh** demotes to a small unobtrusive icon button in the balance card's corner (balances still auto-load on mount + network change, so manual refresh is rarely needed). `Assets` gained `onReceive` + `onEarn` props, wired in `App` to `setReceiveOpen(true)` / `setTab('earn')`. No capability added or removed — this re-weights the surface so the everyday actions are the visual priority. Verified via typecheck + lint + full suite (419) + `build`/`build:mobile` (screens verified via build — repo has no component-test infra). **Remaining #110 (slice 3):** finalize the bottom-nav set (Home · Apps · Activity · Settings) now that Send/Earn are Home actions, + consistent overlay chrome / empty-state polish. (part of #110)
- 2026-07-11 — Earn: **Lantern's own Blend pool — deployed on Testnet + wired into the Earn directory** (`scripts/deploy-lantern-pool.sh`, `core/blend/directory.ts`, `popup/screens/Earn.tsx`, closes #109). Option A from the issue: Blend v2 is a pool *factory*, so "our own pool" is an **instance we deployed** through it — not a bespoke lending contract — which means it speaks the standard Blend ABI and the **entire existing Earn stack (`core/blend/{pool,submit,positions,apr}.ts`) works against it unchanged**; the only app change is the directory entry + branding. **Actually deployed + validated on live testnet** (the prior review had assumed this couldn't be done from the repo — it can, via the `stellar` CLI): a repeatable **`scripts/deploy-lantern-pool.sh`** runs the full bring-up — friendbot-fund a deployer → `poolFactoryV2.deploy` (admin=us, curated params, the shared testnet mock oracle) → `queue_set_reserve`+`set_reserve` for **USDC + XLM** with standard Blend reserve config → `set_status(3)` ("on-ice", which enables supply/withdraw for Earn **without** needing a backstop, since borrow is out of scope) → validate each reserve via a live `get_reserve` simulation. The resulting pool **`CC4KSBTT…`** was proven end-to-end: both reserves resolve via `get_reserve`, a real **supply** of 100 XLM (`submit`, request_type Supply) landed and `get_positions` reported it, and a **withdraw** of 40 XLM round-tripped (position 100→60) — the same `submit` path the wallet uses. `BLEND_POOLS` gains a **`lantern-earn`** entry (`lantern: true`, `verified: true`, the deployed `poolId`, USDC/XLM reserves); a new `lantern` flag on `BlendPool` drives **first-in-list ordering** (`blendPoolsForNetwork` now sorts Lantern pools ahead of third-party ones) and a **"Lantern" badge** (amber lightbulb pill) in the Earn picker, so users see it as *our* pool, distinct from the still-listed third-party `Blend V2 Testnet Pool` (no ABI regression). **No fake addresses:** mainnet is intentionally omitted until a real mainnet pool is deployed + backstop-capitalized (ops) — `blendPoolsForNetwork('public')` stays empty. Directory/label logic unit-tested (Lantern entry shape, Lantern-first ordering with the third-party pool still present, exactly-the-Lantern-pools branding, no-mainnet-placeholder); typecheck + lint + full suite (417) + `build`/`build:mobile` green; scan/sign gating unchanged (supply/withdraw flow through the identical `prepareBlendSubmit` → scan → SIGN_AND_SUBMIT path). **Testnet note:** testnet resets periodically wipe the pool — re-run the script and update the `poolId`. (closes #109)
- 2026-07-11 — UX: **Settings/account hub + decluttered app bar** (`popup/screens/Settings.tsx`, `popup/components/AppBar.tsx`, `popup/App.tsx`, `shared/network.ts`, part of #110 — slice 1, the biggest declutter win). A **Phantom-style** primary surface: the top app bar's crowded **six-icon action row** (Receive · Activity · Cash in/out · Guardians · Security-scan · Lock) is **gone**, replaced by a single **gear → Settings** entry; the bar is now just identity (avatar + copy-address) + the **network badge** (kept always-visible for the BRAND §8 testnet/mainnet distinction, but now **display-only** — the toggle moved into Settings) + gear. Everything advanced is **re-homed, not removed**, into a new grouped **Settings hub** with progressive disclosure: **Account** (address & copy, Receive/QR, Activity), **Security** (Guardians & recovery, Security & scam check, **Auto-lock timer** — new UI), **Cash** (Cash in/out via anchors), **Network** (Testnet/Mainnet segmented toggle + an **Advanced** disclosure), **About** (version, Lock wallet). **Before/after entry points** (nothing becomes unreachable): Receive/Activity/Cash/Guardians/Scan/Lock all move from the app-bar icon row into the corresponding hub group; the network toggle moves from the badge-tap onto Settings › Network; the Send-success flow still auto-opens Activity. **Two genuinely net-new controls:** (1) **Auto-lock** presets (1/5/15/30/60 min + **Never**) — the `armAutoLock` timer was hardened to leave itself **disarmed when minutes ≤ 0** (previously `setTimeout(0)` would have locked instantly), so "Never" is safe; the worker already reads `autoLockMinutes`. (2) **Horizon / Soroban-RPC endpoint overrides** behind the Advanced disclosure, per-network, edited as validated http(s) URLs (blank = pinned default) — made **functional** by a new pure `resolveNetworkConfig(settings)` resolver that App applies at the single chokepoint where it derives the `NetworkConfig` passed to every screen, so an override reaches both reads **and** the `SIGN_AND_SUBMIT` submit path (which takes `horizonUrl` from that same config); an RPC override is ignored on a network with no Soroban support (mainnet) so it can never conjure a fake endpoint. **Security-critical flows untouched** — scan→review→sign gating, guardian recovery, and the testnet/mainnet visual distinction are unchanged; only entry points moved. Pure logic unit-tested (`resolveNetworkConfig`: default pass-through by reference, per-network Horizon/RPC override application, mainnet-RPC ignore, cross-network isolation, blank/whitespace/non-URL rejection); typecheck + lint + full suite (419) + `build` + `build:mobile` green (screens verified via typecheck/lint/build — the repo has no component-test infra). **Remaining #110 slices:** Home action-forward quick-action row (Send·Receive·Swap·Earn in ≤1 tap) + finalize the bottom-nav set (Home·Apps·Activity·Settings) and overlay-chrome polish. (part of #110)
- 2026-07-11 — Android: **wire the biometric plugin into the native project + BiometricPrompt permission** (`android/app/src/main/AndroidManifest.xml`, `android/capacitor.settings.gradle`, `android/app/capacitor.build.gradle`, part of #94 / #23 M2a). The `@capgo/capacitor-native-biometric` adapter (#83) was a JS/npm dependency but had never been `cap sync`'d into the committed Android project, so the native side wasn't actually built in — biometric unlock could not work on device regardless of the JS code. Ran `npx cap sync android`, which registers `capgo-capacitor-native-biometric@8.6.0` in the Gradle project (settings + app build), and added the plugin's required **`USE_BIOMETRIC`** permission (API 28+) plus the legacy **`USE_FINGERPRINT`** fallback (API 23–27) to the app manifest per the plugin docs. This closes the build-verifiable half of #94 (adapter merged + flag ON via #96/#97/#100, now natively wired); the remaining acceptance item is the **human on-device demo** (enrol → lock → biometric unlock → silent cancel-fallback on real Android hardware), which can't run in CI. Vault untouched; the security invariants are unchanged (wrong password can't enrol, biometric-recovered password still runs `decryptSecret`). (part of #94)

- 2026-07-11 — Swaps: **Soroswap best-price aggregator engine** (`core/stellar/soroswap.ts` + Swap screen, part of #71 — slice 5, the last engine slice). Adds best-price routing across **Soroswap / Aqua / Phoenix / SDEX** via the Soroswap API on top of the native SDEX path-payment engine (#72/#74/#76): the Swap screen now quotes **both** engines in parallel and uses the aggregator **only when it returns a strictly better rate** — any failure (no API key, non-2xx, 428-needs-trustline, empty route, malformed body, network error) silently falls back to the native path, so the aggregator can never block a swap. The engine is pure + fail-soft with an **injectable `fetch`**: `fetchSoroswapQuote` (`POST /quote`, EXACT_IN, amount in stroops, Bearer-key auth) → `buildSoroswapSwapXdr` (`POST /quote/build` → ready-to-sign Soroban XDR) → the existing **scan → review → SIGN_AND_SUBMIT** gate (a Soroswap swap decodes as a Soroban `contract_call`, so it's reviewed as a medium-risk contract call; the review sheet shows *You send / Expected / Minimum received / Route: Soroswap — best price* from the quote). Assets are mapped to their **Soroban SAC contract ids** via `Asset.contractId(passphrase)` (unit-verified against the id Soroswap's live testnet token list publishes for XLM), with `toStroops`/`fromStroops` (BigInt, no float error) and a pure `pickBestEngine` comparator (ties/unusable → the reliable native SDEX). **Revenue share** wired per the API contract: `feeBps` on the quote + `referralId` (our wallet) on the build, sent **only when both are configured**. Gated behind the existing **`swapAggregator` flag + `VITE_SOROSWAP_API_KEY`** (a secret) — unset on web/store builds today, so the wallet keeps swapping via native SDEX until a key is provisioned. Fully unit-tested offline (13 cases: SAC mapping incl. the live XLM id, stroop codec + rejections, best-price/tie/fallback selection, quote+build request shape/auth/fee-gating, and fail-soft on 403/428/500/empty/malformed/network). **Remaining #71 acceptance item (human, can't run in CI):** a live-API round-trip with a real key + the end-to-end Testnet swap demo. (part of #71)
- 2026-07-10 — Earn UI: **withdraw-all, est. supply APY, and position hero** (`popup/screens/Earn.tsx` + new `core/blend/apr.ts`, part of #92). Three improvements to the Blend "Earn yield" surface. **(1) Withdraw all:** the withdraw amount input now has a **MAX** affordance prefilled from the user's live position (bTokens→underlying via `b_rate`, reusing `readSuppliedPositions`); tapping it submits a **full-balance sentinel** (`BLEND_WITHDRAW_ALL_AMOUNT` = `u64::MAX`, in `core/blend/pool.ts`) rather than the displayed underlying. Blend's `Withdraw` caps the request to the exact bToken balance, so the sentinel **leaves no dust** where a rounded display value would strand the sliver that accrues between quote and submit — and `u64::MAX` (not `i128::MAX`) is used on purpose because the contract ceil-divides `amount × SCALAR_12` to bTokens and `i128::MAX × 1e12` would overflow/panic, whereas `u64::MAX × 1e12 ≈ 1.8e31` stays well under `i128::MAX`. Editing the amount clears the full-withdraw flag (partial). **(2) Est. supply APY:** a new PURE, unit-tested `estimateSupplyApy()` mirrors Blend v2's on-chain interest-rate model (`pool/src/pool/interest.rs`) — utilization `U = (d_supply·d_rate)/(b_supply·b_rate)`, a three-slope kinked borrow APR keyed off the reserve's target utilization × `ir_mod`, `supplyApr = borrowApr·U·(1−backstopTake)`, compounded weekly to APY — reading the reserve's rate params from the existing `get_reserve` sim path (`readReserveApys`, sharing `simulateView` with `positions.ts`). Surfaced as **"est. APY"** on the pool picker, the position hero, and the supply/withdraw sheet. **Fail-soft**: any RPC/decode error or missing field shows **"—"**, never a wrong number or a blocked flow (the backstop take rate defaults to 0 — a documented small over-estimate — since reading it is a separate fragile call). **(3) Visual polish:** a **"Your positions"** amber-glow hero (`shadow-layer-1`, `ring-primary-container`, per-asset earning + est. APY) when the user has supplied balances, a consistent empty state (*"Nothing earning yet — supply an asset to start"*), and a clear **"Available pools"** section below. **Scan gating unchanged** — the full-withdraw sentinel and every other amount flow through the exact same `prepareBlendSubmit` → scan → SIGN_AND_SUBMIT path; no gate is bypassed. APY math unit-tested against fixture reserve data (three-slope boundaries, ir_mod scaling, backstop take, accrued rates) + fail-soft null on RPC errors; the screen is verified via typecheck/lint/build (repo has no component-test infra). (part of #92)
- 2026-07-10 — Navigation: **Earn moves into the bottom nav, Activity moves to the top app bar** (`popup/components/BottomNav.tsx`, `popup/components/AppBar.tsx`, `popup/App.tsx`, `popup/screens/Earn.tsx`, `popup/screens/Activity.tsx`, part of #91). The bottom nav is now **Assets · Earn · Send · Apps** — the `Tab` type swaps `activity` for `earn` and the nav item uses the `savings` icon with the amber active-pill highlight. `Earn` gained an **`embedded` variant**: a shared `Shell` renders each step (pick/form/review/success) either as the existing full-screen overlay *or* inline inside the tab with no back-header at the picker and an internal back affordance on the deeper steps; "Done"/close resets the flow to the pool picker instead of closing. **Activity** moved to a `history`-icon app-bar action ("Activity") that opens it as a full-screen overlay with a back header (reusing the Receive/Guardians pattern) — it took an `onBack` prop and now wraps its list (and the `TxDetail` drill-in, which still works) in full-screen chrome. `App.tsx` repurposes the old `earnOpen` overlay state into `activityOpen`, routes the Earn tab to render `<Earn embedded />`, and points the Send success `onDone` at the Activity overlay so a fresh tx is still visible. The stale `savings` app-bar action and `onOpenEarn` prop were removed (renamed to `onOpenActivity`). (part of #91)
- 2026-07-10 — Apps: **"install"/favorite mini-apps + a "My apps" section, and a listed Lantern Demo App** (`core/miniapps/favorites.ts`, `core/miniapps/directory.ts`, `popup/screens/Apps.tsx`, `shared/types.ts`). Each directory card gains a bookmark toggle (install/remove); favorited ids persist in `Settings.favoriteApps` over the existing KV port and render in a **My apps** section pinned above **Discover**, live-updating across surfaces via `onSettingsChanged`. Favoriting is a **bookmark only** — no new capabilities: launch still goes through the sandboxed frame + connect + scan-gated sign path, identical to an un-favorited app. Favorites logic is a pure/testable module (`isFavorite`/`add`/`remove`/`toggle`/`orderedFavoriteApps` + a Settings round-trip) with unit tests. Adds a **remote** MiniApp shape (`url` alongside bundled `path`) and a directory entry for the **Lantern Demo App** (`https://lanternmock.up.railway.app/`, launched in the opaque-origin sandbox like the URL bar, passes the same `https` scheme check). The demo entry is **always-on, NOT gated behind `DEMO_AFFORDANCES`** — that flag hides things unsafe/misleading in production (faked scan verdicts, a hardcoded flagged-address deny-list); a real sandboxed https app is neither, and it exists to be shown in public demo builds where the flag is off (rationale in a code comment on the entry). (part of #93)
- 2026-07-10 — Fix: **native biometric adapter — no unhandled rejection / false-positive availability off-device** (`core/crypto/biometric-store-native.ts` + `tests/messages.test.ts`, follow-up to #96/#83). `loadPlugin()` resolved straight to the `@capgo/capacitor-native-biometric` Capacitor proxy, so `await loadPlugin()` made the promise machinery probe `proxy.then` — the web shim throws `UNIMPLEMENTED`, surfacing as an unhandled rejection that escaped every caller's try/catch. The proxy is now **boxed** in a plain, non-thenable object (`{ plugin }`) and destructured at each call site, so only its awaited *method* calls (never the proxy itself) are awaited. Separately, the native message-routing test faked `isNativePlatform()` on web, routing `GET_STATUS` into the real adapter (whose web shim reports biometrics *available*); it now pins a deterministic unavailable store via the existing `__setBiometricStore` seam, isolating routing from ambient device biometric state. This surfaced because CI runs only the Android build, not `npm test`; full suite (374) + typecheck + lint + `build`/`build:mobile` green with `@capgo` installed via `npm ci`. (part of #23)

- 2026-07-08 — Security: **native biometric-store adapter (Android Keystore)** (`core/crypto/biometric-store-native.ts`, part of #23 Milestone 2a). The real `BiometricStore` the session handler uses on device — completes the M2a code chain (#78 crypto → #79 orchestration → #80 session → #82 UI → this). Holds the 256-bit wrapping key in the platform Keystore/Keychain via **`@capgo/capacitor-native-biometric`** (the Capacitor-8-compatible fork, matching the project's Cap 8.4.1), released only after a successful biometric `verifyIdentity`: `setKey` → `setCredentials` (base64 key), `getKey` → `verifyIdentity` (the prompt) then `getCredentials` → decode (returns `null` on cancel/lockout so unlock fails soft), `clearKey` → `deleteCredentials`, `isAvailable` requires both a usable biometric **and** a secure lock screen (so the Keystore key is hardware-protected). `getBiometricStore()` now returns this on native (`isNativePlatform()`) and keeps the unavailable stub on extension/web (a WebAuthn-PRF adapter is a later follow-up). The plugin is loaded through a **variable-specifier `@vite-ignore` dynamic import**, so the web/extension bundle never pulls it in and the app **typechecks + builds without the package installed**; the pure base64 key codec (`keyToCredential`/`credentialToKey`, strict 32-byte validation) is unit-tested off-device (round-trip, all byte values, wrong-length + corrupt-input → `null`). **Human wiring (device-only, can't run in CI):** `npm install @capgo/capacitor-native-biometric && npx cap sync android`, add the `USE_BIOMETRIC` Android permission per the plugin docs, then verify the enroll → lock → biometric-unlock flow on real hardware — the last M2a acceptance item ("ships on at least Android"). (part of #23)

- 2026-07-10 — Passkey: **seed-phrase-free Soroban smart accounts** (final #53 slices — contract, deploy/bind, signing route, onboarding, docs; closes #53 / #23 M2b). The signing key IS a device passkey: a minimal Rust **custom-account contract** (`contracts/passkey-account`, soroban-sdk 27) binds a 65-byte SEC1 P-256 key in `__constructor` and its `__check_auth` verifies WebAuthn assertions via **`secp256r1_verify`** — signature over `authenticatorData ‖ SHA-256(clientDataJSON)` **plus** a strict check that clientDataJSON's `"challenge"` equals base64url(signature payload), so an assertion can never authorize a different payload (5 Rust unit tests incl. wrong-challenge/wrong-key). The 3.2KB WASM is **vendored** (`core/passkey/contractWasm.ts`, generated by `scripts/build-passkey-contract.sh`) so MV3 bundles it and CI needs no Rust. New core: `smartAccount.ts` (upload + CreateContractV2 builders, deterministic C… prediction via the contract-id preimage), `authEntry.ts` (Soroban auth-entry payload hash → WebAuthn challenge → signature ScVal; payload hash **cross-checked against the SDK's own `authorizeEntry`**), `normalizeLowS` (authenticators may emit high-S), `sac.ts` + `getLedgerEntries` (contract XLM balance without a funded source; SAC `transfer` builder), `onboard.ts` (**no-mnemonic onboarding**: register passkey → ephemeral friendbot-funded deployer → upload → deploy; the deployer secret never leaves function scope), and `transfer.ts` (prepare/finalize split so the **scan → approve gate** sits between build and passkey-sign — no bypass lane — with a re-simulation in enforce mode before broadcast). UI behind the new `VITE_FEATURE_PASSKEY` flag (default OFF, testnet-only, hidden on native): "Create with Passkey" onboarding path that never generates or displays a mnemonic, and a SmartAccount screen (balance, scan-gated send authorized by the passkey prompt, forget-on-device). **Proven on live testnet** by `tests/passkey-e2e.live.test.ts` (fake WebCrypto authenticator, CI-skipped): deploy `357e8b97…`, fund `82b3c9b0…`, **passkey-authorized send `589cfec9…`**. Guardian-recovery interop (#23 M1) documented in `docs/passkey-smart-account.md` (guardians become contract-level co-signers via a rotation entrypoint — the tracked follow-up, along with mainnet fee sponsorship). (part of #53)

- 2026-07-08 — Passkey: **WebAuthn register / sign wrappers** (`core/passkey/webauthn.ts`, part of #53 — slice 3). The device-facing layer that ties the pure crypto (#87) + assertion (#88) helpers into the full client-side passkey flow. `registerPasskey({ rpId, rpName, userId, userName, challenge })` calls `navigator.credentials.create` requesting a **resident key + user verification + ES256 only**, then returns the **65-byte SEC1 public key** (via `p256PublicKeyFromSpki`) to bind as a Soroban smart-account signer, plus the credential id — rejecting a cancelled prompt or a non-ES256 authenticator. `signWithPasskey({ rpId, credentialId, challenge })` calls `navigator.credentials.get` and returns the assertion the contract verifies: `authenticatorData`, `clientDataJSON` (which embeds the challenge), and the signature **converted to raw 64-byte r‖s** (via `derToRawEcdsaSignature`). The `credentials` API is **injectable**, so the wrapper logic is unit-tested without a real authenticator: registration returns the SEC1 key + id from a fake ES256 credential (and rejects cancel / RS256), and `signWithPasskey`'s raw signature **verifies end-to-end** over `webauthnSignedMessage(authenticatorData, clientDataJSON)` against the registered key. The `navigator.credentials` default path is exercised on-device (a real authenticator can't run in CI). This completes the **client-side** passkey chain (#87 formats → #88 assertion → this); the remaining piece is the Soroban **secp256r1 smart-account contract** (deploy/bind + no-mnemonic onboarding), which needs a live testnet + a passkey device. (part of #53)

- 2026-07-08 — Passkey: **WebAuthn assertion helpers** (`core/passkey/assertion.ts`, part of #53 — slice 2). The pure, offline-testable core of "sign a Stellar op with a passkey." A WebAuthn authenticator signs the bytes **`authenticatorData ‖ SHA-256(clientDataJSON)`** — *not* the raw challenge — and the payload we actually want signed (a tx hash) rides inside `clientDataJSON`. `webauthnSignedMessage(authenticatorData, clientDataJSON)` reconstructs those exact signed bytes (for local verify + the Soroban secp256r1 contract); `parseClientData` extracts `type`/`challenge`/`origin`; and `challengeMatches(clientDataJSON, expected)` confirms the assertion is a `webauthn.get` whose base64url challenge equals **our** payload — guarding against a replayed or swapped assertion before the signature is trusted. No `navigator.credentials` calls (those thin device-gated forwarders are a later slice). Unit-tested with WebCrypto: `parseClientData` extract + malformed/missing-field rejection; `challengeMatches` accept vs wrong-challenge/`webauthn.create`/garbage; and `webauthnSignedMessage` both equals `authData ‖ SHA-256(clientData)` **and** produces a message a P-256 key signs and that then verifies. Next: the `navigator.credentials` register/sign wrappers + the Soroban secp256r1 smart-account contract bind (device/testnet-gated). (part of #53)
- 2026-07-08 — Passkey: **secp256r1 WebAuthn↔Soroban format primitives** (`core/passkey/secp256r1.ts`, part of #53 — first slice; unblocked now that #21's Soroban invoke builder shipped). The offline-testable crypto core for seed-phrase-free passkey signing (#23 M2b): a WebAuthn passkey is a NIST **P-256 (secp256r1)** credential, but a Soroban secp256r1 smart account binds the **raw** public key and verifies **raw** `r‖s` signatures — while the browser hands over an **SPKI-DER** key and **ASN.1-DER** signatures. Two pure converters bridge that: `p256PublicKeyFromSpki(spki)` imports the SPKI via WebCrypto (which *validates* it's a real P-256 point, rejecting junk) and exports the **65-byte uncompressed SEC1** key; `derToRawEcdsaSignature(der)` parses `SEQUENCE { INTEGER r, INTEGER s }` into the fixed **64-byte** `r‖s` form Soroban expects — stripping DER sign bytes, left-padding short integers, and rejecting malformed input (bad SEQUENCE/INTEGER tags, length mismatch, trailing bytes, oversized integers). No WebAuthn/native calls (registration + the smart-account contract are later slices) — pure crypto. Unit-tested with WebCrypto fixtures: SPKI→SEC1 matches WebCrypto's own raw export + rejects non-P-256; a real signature round-trips DER→raw and **still verifies**, plus explicit padding + malformed-signature cases. Next: WebAuthn registration/assertion wrappers, the Soroban smart-account contract deploy/bind, and the no-mnemonic onboarding path (device-/testnet-gated). (part of #53)
- 2026-07-08 — Build: **gate the biometric surface behind its flag + flags README** (`core/session/handler.ts` + README, part of #81 — final slice). `GET_STATUS` now reports `biometricEnabled`/`biometricAvailable` as **false unless `__FEATURE_BIOMETRIC_UNLOCK__`** is on, so a default (flag-off) build never offers the in-progress (#23 M2a) biometric surface on the Unlock screen — keeping it out of store builds until it's device-verified, exactly the flag's purpose. (The `GEOVELOCITY` flag stays **reserved with no surface to gate**: #70 was closed as not-planned, so no geo-velocity code exists yet — the guard goes in when that feature is built.) Adds the README **"Build-time feature flags"** section: the full flag table + defaults, `FLAGS.<name>` (behavior) vs `__FEATURE_<NAME>__` (tree-shaking) usage, how to flip one per build (`VITE_FEATURE_… npm run build`), and that both targets honor the same flags with `npm run verify:flags` proving DCE. typecheck, lint, build, and the 306-test suite green (biometric tests still pass — the test runner defines all flags on). **Completes #81's criteria** (module + defaults, DCE proof, shared config helper, `.env.example`, DEMO_AFFORDANCES + BIOMETRIC_UNLOCK gating, both builds green, README) — the epic can close once the #84→#85→this chain merges. (part of #81)

- 2026-07-08 — Build: **feature-flag Vite wiring + dead-code-elimination proof** (`vite.flags.ts`, `src/shared/flag-defs.ts`, both vite configs, part of #81 — slice 2). Makes the flags (#84) actually gate the *build*, not just runtime behavior. A single-source `flag-defs.ts` (each flag's `VITE_FEATURE_*` env var, its `__FEATURE_*__` define literal, and safe default) now drives both `shared/flags.ts` (runtime `FLAGS`) and a shared `vite.flags.ts` `flagDefines()` helper that **both `vite.config.ts` and `vite.config.mobile.ts`** consume (via `loadEnv` → `define`), so the two builds can't drift; `vitest.config.ts` defines all literals ON so every gated path stays tested. Guarded code reads `if (__FEATURE_DEMO_AFFORDANCES__)` (declared in `src/feature-flags.d.ts`) so a disabled flag's branch **and its imports** tree-shake out. First real gate: **`DEMO_AFFORDANCES` now compiles out** `ScanContext.forceScenario` (a production build can't be told to fake a scan verdict) and the demo deny-list (`DEMO_FLAGGED_ADDRESSES` + the Scan-screen demo gallery, extracted to a gated `<DemoWarnings>`). **Externally-verified DCE:** a new `npm run verify:flags` (`scripts/verify-flags-dce.mjs`) builds the bundle with the flag off and on and asserts the demo deny-list address is **absent from the emitted JS when off, present when on** — proving the define strips code, not just skips a branch (ignores sourcemaps, which retain original source). Unit-tested (`flagDefines` env/default mapping + `allFlagDefinesOn`); typecheck, lint, `build`, `build:mobile`, and the full 306-test suite green. Remaining for #81: gate the `GEOVELOCITY` / `BIOMETRIC_UNLOCK` surfaces behind their flags + the README section. (part of #81)

- 2026-07-08 — Build: **feature-flags module** (`src/shared/flags.ts` + `.env.example`, part of #81 — first slice). The single source of truth for build-time gating of optional / external / in-flight surfaces — no such layer existed (zero `import.meta.env` reads in `src/`). A pure `parseFlag(value, defaultOn)` resolves one flag (unset/empty → the safe default; `"true"`/`"1"` → ON; anything else → OFF), and `FLAGS` is a **frozen** typed object of `VITE_FEATURE_*` reads with safe defaults: shipped-core ON (`swap`, `earnBlend`, `anchors`, `miniapps`), external/unfinished/demo OFF (`swapAggregator`, `biometricUnlock`, `geovelocity`, `demoAffordances`). Call sites read `if (FLAGS.geovelocity) { … }`; disabled features can dead-code-eliminate (a Vite-`define` literal to force tree-shaking of external SDKs is a follow-up slice). `.env.example` documents every flag + default + a one-line description (booleans only, never secrets). Pure/unit-tested (`parseFlag` default/ON/unknown-ignored cases; `FLAGS` is frozen with the documented ON/OFF defaults). Follow-up slices: the shared `vite.config.ts` + `vite.config.mobile.ts` flag→define helper + a DCE proof, gating the `GEOVELOCITY`/`BIOMETRIC_UNLOCK`/`DEMO_AFFORDANCES` surfaces behind their flags, and the README section. (part of #81)

- 2026-07-08 — Security: **biometric unlock — Unlock-screen UI** (`popup/screens/Unlock.tsx` + `GET_STATUS.biometricAvailable`, part of #23 Milestone 2a). The user-facing half of biometric unlock, wiring the session messages (#80) into the lock screen: when biometric is **enrolled**, the Unlock screen shows an **"Unlock with biometrics"** button (with a `fingerprint` icon) *and auto-presents the prompt once on open* — success unlocks; a **cancel is silent** (fall back to the password field), any other failure shows an inline message. When the device **supports** biometrics but they aren't enrolled yet, the password form shows an **"Enable biometric unlock on this device"** opt-in checkbox that, on a successful password unlock, enrols via `ENABLE_BIOMETRIC` using the just-verified password (best-effort — a failure never blocks the unlock). `GET_STATUS` gains a `biometricAvailable` flag (`getBiometricStore().isAvailable()`) so the opt-in only appears on capable devices; `App` passes both flags to `Unlock`. Because the store is still the *unavailable* stub (no native adapter yet), both flags read false on every platform today, so the lock screen is unchanged in practice — the UI activates the moment the native Android-Keystore adapter lands. Screen verified via typecheck/lint/build (repo has no component-test infra); the two `GET_STATUS` shape assertions updated for the new flag; full suite 298 green. This completes the **build-verifiable** M2a surface — the only remaining piece is the **native biometric adapter** (a Capacitor plugin dependency + on-device verification). (part of #23)

- 2026-07-08 — Security: **biometric unlock — session-handler path** (`core/session/handler.ts` + `core/crypto/biometric-store.ts` + message types, part of #23 Milestone 2a). Wires the biometric orchestration (#79) into the worker: three new messages — **`ENABLE_BIOMETRIC`** (re-verifies the password against the vault via `decryptSecret` *before* wrapping it, so a wrong password can never be enrolled → `BAD_PASSWORD`), **`BIOMETRIC_UNLOCK`** (runs `biometricUnlock`, then the recovered password still goes through the normal vault `decryptSecret` → a bad/tampered envelope can't yield access), and **`DISABLE_BIOMETRIC`** — plus a `biometricEnabled` flag on `GET_STATUS` so the Unlock screen knows whether to offer the button. `BIOMETRIC_UNLOCK` returns typed failure codes (`NOT_ENROLLED` / `BIOMETRIC_CANCELLED` / `BIOMETRIC_FAILED`) so the UI fails soft to the password field. A new `getBiometricStore()` platform selector (mirroring `getKV`/`__setKV`, with a `__setBiometricStore` test seam) returns an **unavailable** store on every platform for now — **no native dependency added** — so biometric unlock stays inert (never enrols; unlock reports cancelled) until the native Android-Keystore adapter fills it in a follow-up slice. Fully unit-tested by driving `handle()` with an in-memory KV + a fake store (enable → lock → unlock round-trip; wrong-password enrol rejected with nothing stored; not-enrolled; dismissed-prompt → cancelled + stays locked; rotated-key → failed; disable clears enrolment); the two `GET_STATUS` shape assertions updated for the new flag. The vault is untouched. Remaining M2a: the native Keystore/WebAuthn-PRF adapter (device-verified) + the Unlock button & settings toggle. (part of #23)

- 2026-07-08 — Security: **biometric-unlock orchestration + store seam** (`core/crypto/biometric-unlock.ts`, part of #23 Milestone 2a). Ties the envelope crypto (#78) to two seams so the flow is platform-agnostic + unit-testable: a **`BiometricStore`** interface (`isAvailable`/`setKey`/`getKey`/`clearKey`) that the native Android-Keystore adapter (next slice) implements — `getKey` triggers the biometric prompt and resolves to the 256-bit wrapping key on success or `null` on cancel/unavailable — plus ordinary `KV` for the wrapped-password blob (`lantern.biometric`). `enableBiometricUnlock(password, deps)` mints a fresh wrapping key → stores it → wraps the (already-verified) password → saves the blob; `biometricUnlock(deps)` reads the blob → biometric-reads the key → unwraps, returning a **typed** `{ ok, password } | { ok:false, reason:'not-enrolled'|'cancelled'|'failed' }` so the UI can fail soft to the password field (a cancelled prompt never errors; a `failed`/tampered blob never yields access — the recovered password still runs the normal `UNLOCK`/`decryptSecret`). `disableBiometricUnlock` clears both key + blob; `isBiometricEnabled` checks the blob. Nothing weakens the PBKDF2/AES-GCM vault. Fully unit-tested with in-memory KV + fake store (enable→unlock round-trip; not-enrolled; cancelled prompt; key-rotated-out → failed; unreadable-blob → failed; disable clears both). Follow-up slices: the native Keystore/WebAuthn-PRF adapter, a `BIOMETRIC_UNLOCK` session path, and the Unlock-screen button + settings toggle (device-verified). (part of #23)

- 2026-07-08 — Security: **biometric-unlock envelope crypto** (`core/crypto/biometric.ts`, part of #23 Milestone 2a). The pure security core for gating the existing vault behind platform biometrics instead of a typed password each time — **without weakening the vault**: the PBKDF2/AES-GCM vault (`crypto/vault.ts`) is untouched; this adds a *second* envelope so the password can be released after a biometric check. Design (prime directive — no secret leaves the device): a random 256-bit **wrapping key** lives in the device's biometric-gated secure store (Android Keystore via a native adapter, a follow-up slice), and the vault **password** is AES-GCM-encrypted under it (`wrapSecret`) with the blob kept in ordinary storage — useless without the wrapping key, which only a successful biometric auth can read. So biometric unlock = get wrapping key → `unwrapSecret` → run the normal `UNLOCK` (`decryptSecret`) path; keeping only the small key in the hardware-backed store (bulk stays in KV) is the standard envelope pattern. `generateWrappingKey` (256-bit), `wrapSecret`/`unwrapSecret` (fresh IV per wrap; `unwrapSecret` throws `BiometricUnwrapError` on any auth-tag mismatch, never revealing wrong-key vs tamper, never echoing ciphertext), key-length validation. Pure Web-Crypto, no native calls / no storage — fully unit-tested (round-trip; distinct random keys; fresh-IV non-determinism; wrong-key + tampered-ciphertext + bad-key-length rejection). Follow-up slices: the native biometric adapter (Keystore get/set/remove), the enable/disable orchestration + a `BIOMETRIC_UNLOCK` session path, and the Unlock-screen button + settings toggle — the device-verified pieces. (part of #23)

- 2026-07-07 — Swap UI: **in-wallet swaps wired end-to-end** (`popup/screens/Swap.tsx` + a Swap action on Assets, part of #71 — slice 4). The user-facing swap flow, composing the merged primitives: a new **Swap** button on the Assets screen opens a full-screen overlay where you pick a **From** asset (any you hold) + amount and a **To** asset (any other you hold a trustline for), then **Get Quote** fetches the best route from Horizon (`fetchStrictSendPaths` #75), applies a **0.5% slippage floor** (`destMinFromQuote` #72), builds the strict-send path payment (`buildPathPaymentStrictSendXdr` #72), and runs it through the wallet's existing **scan → approve → SIGN_AND_SUBMIT** path. The review sheet shows *You send / Expected (~quote) / Minimum received (0.5% slippage)* and the Lantern scan verdict (`RiskCallout`/`ScanBadge` + the high-risk CONFIRM / press-and-hold gate reused from Send), so the swap-and-send risk + plain-language swap label (slice 3, #76) surface here once merged. **Graceful degradation** per the #71 criterion: no route / zero liquidity shows a clear "not enough liquidity" message rather than a broken prompt; balances refresh on return (the Assets screen reloads). Dest options are limited to assets you already hold a trustline for (a strict-send self-swap requires the receive trustline); with only XLM held, the form prompts to add a trustline first. Screen verified via typecheck/lint/build (repo has no component-test infra); the quote/build/slippage/scan primitives it composes are unit-tested in #72/#75/#76. Remaining for #71: the Soroswap aggregator engine (live-API-validated) + the end-to-end Testnet swap demo (needs a funded, trustlined account). (part of #71)
- 2026-07-07 — Scan: **plain-language swap labels + swap-and-send risk** (`core/scan/{decode,explainer,engine,types}.ts`, part of #71 — slice 3). Satisfies #71's "scanned & reviewed … asset in/out + amounts + min-received in plain language, not the generic payment wording." The decoder now exposes both sides of a path payment — `sendAssetCode`/`sendAmount` and `destAssetCode`/`destMin` (strict-send: exact send + received floor; strict-receive: max spend + exact receive) — fixing the prior generic case that paired the send *amount* with the *dest* asset. The explainer reads a strict-send as **"This swaps 100 XLM for at least 24.3 USDC."** (naming both assets, the amount, and the slippage floor) instead of "swaps assets and sends about …". The engine adds a **`swap_to_other` medium-risk reason** when a swap routes its proceeds to a *different* account than the sender (a "swap and send" — the funds leave the wallet; a drainer could disguise a transfer as a swap); a normal self-swap stays low-risk `allow`. Pure, unit-tested by round-tripping a real `buildPathPaymentStrictSendXdr` (#72) XDR through decode → explain → scan (both-sides + floor decode; exact swap wording, no generic regression; self-swap allow vs swap-to-other medium). Next: the Swap UI (quote → review → sign) + the Soroswap aggregator. (part of #71)

- 2026-07-07 — Swaps: **strict-send path discovery / quote** (`core/stellar/paths.ts`, part of #71 — slice 2). Feeds the swap builder (#74) a live quote: `fetchStrictSendPaths({ horizonUrl, sendAsset, sendAmount, destAsset, fetchImpl? })` asks Horizon's `/paths/strict-send` for the best receivable amount + route (SDEX order books / AMM pools) when spending an exact amount, returning `{ destAmount, path }` — `destAmount` → `destMinFromQuote` (the slippage floor #74) and `path` → the path payment's intermediate hops. Picks the record with the **highest `destination_amount`** (best price) and maps each hop's Horizon asset to an `AssetRef`. **Graceful degradation** per the #71 criterion: returns `null` when Horizon has no route (zero liquidity) so the UI can say "no path" rather than error; a real network / non-2xx failure throws so the caller can distinguish "no path" from "lookup failed". Injectable `fetch` (not the SDK `Server`), so unit-tested offline (best-of-N selection, issued-asset hop mapping, query-param encoding for native + issued send assets, empty-records → `null`, non-2xx → throws); the query format was also sanity-checked against live testnet Horizon. Next: swap scan/explain label + the Swap UI wiring quote → build → sign. (part of #71)

- 2026-07-07 — Blend: **supplied-position readout** (`core/blend/positions.ts` + Earn screen, part of #21). Completes #21's "supply … **and see it reflected as a yield-bearing position**" criterion: the Earn pool picker now shows *"N USDC earning"* under each reserve the user has supplied. `readSuppliedPositions(poolId, user, reserves, { rpcUrl })` **read-only** simulates the pool's `get_positions` + `get_reserve` view functions against a Soroban RPC (no tx built, nothing signed) and converts Blend's stored **bTokens** (interest-bearing shares, keyed by reserve *index*) into the underlying asset amount via each reserve's live `b_rate`: `underlying = bTokens × b_rate / 1e12` (SCALAR_12), the pure `bTokensToUnderlying()` helper. **Fail-soft**: any RPC/decode error yields `null` so the readout is simply omitted — it never blocks supply/withdraw. Shapes were validated live against soroban-testnet (`get_positions` → `{collateral,liabilities,supply}` index→bToken maps; `get_reserve.config.index`/`.decimals` + `data.b_rate`; USDC reserve index 3, b_rate ≈ 1.0558). Unit-tested with SDK-built ScVal fixtures (`bTokensToUnderlying` identity/growth/zero; `readSuppliedPositions` per-reserve underlying incl. a zero position; all-zero for a fresh account; **fail-soft null** on RPC error). The picker load is non-blocking + memoized (no refetch loop). The one remaining #21 item is the **live end-to-end Testnet demo** (supply → watch the readout → withdraw), which needs a human with a USDC/XLM-funded account. (part of #21)
- 2026-07-07 — Stellar: **path-payment swap builder** (`buildPathPaymentStrictSendXdr` in `core/stellar/swap.ts`, part of #71). First slice of in-wallet swaps: builds the *unsigned* XDR for a market swap via a **strict-send path payment** — send exactly `sendAmount` of `sendAsset`, receive at least `destMin` of `destAsset`, routed through `path` across the native SDEX order book + AMM pools. A swap is a self-payment (destination defaults to the source), and `destMin` is the **slippage floor** the network enforces atomically (a price move can't silently give a worse rate). Validates strictly-positive amounts (≤7dp), distinct send/dest assets, and well-formed asset refs; reuses the existing `AssetRef` shape. Ships `destMinFromQuote(quoted, tolerance)` to compute the floor from a quote. Pure/offline — signing + scan run through the existing worker path; the decoder already recognizes `pathPaymentStrictSend`. Unit-tested (self-swap XLM→USDC round-trip, custom destination, intermediate path, issued→issued, identical-asset/amount/precision/missing-issuer rejections, slippage-floor math). Next: Horizon strict-send path discovery, swap scan-labeling, the Swap UI, and the Soroswap aggregator engine. (part of #71)
- 2026-07-07 — Blend UI: **"Earn yield" supply/withdraw screen, wired end-to-end** (`popup/screens/Earn.tsx` + new `core/blend/submit.ts`, part of #21). The user-facing capstone of the Blend integration: a new `savings` AppBar action opens a full-screen overlay (same pattern as Guardians/CashInOut) that lists the curated Blend pools for the network (`blendPoolsForNetwork` #68) with per-reserve **Supply**/**Withdraw** actions, takes an amount, and runs it through the wallet's existing **scan → approve → SIGN_AND_SUBMIT** path. The core glue is a new pure, unit-tested `prepareBlendSubmit()` — the **build → simulate → assemble** pipeline for a Blend `submit`, mirroring `prepareInvoke` (#65) but for Blend's nested `Request` call shape (which the scalar `InvokeArg` path can't express, so `prepareInvoke` can't be reused): it builds the pre-sim tx (`buildBlendSubmitXdr` #67) → simulates against the network's Soroban RPC (`simulateTransaction` #56, capturing footprint + auth + resource fee) → assembles the ready-to-sign XDR (`assembleInvokeXdr` #64), returning `{ ok, xdr } | { ok:false, error }` so the screen surfaces one thing. A pure `toBaseUnits(human, decimals)` converts the entered amount to the i128 base units the builder needs (rejecting over-precision / non-positive input). The prepared tx is **scanned before signing** (advisory `RiskCallout`/`ScanBadge` + the high-risk CONFIRM / press-and-hold gate reused from Send), and the review sheet spells out the action/pool/asset/amount in plain language. `prepareBlendSubmit` + `toBaseUnits` are unit-tested offline with an injected `fetch` (build→sim→assemble happy path → decodes to `submit` on the pool, fee = inclusion+resource; contract-revert → `ok:false`; bad amount → `ok:false` **without** calling the RPC; non-2xx RPC and missing-footprint → `ok:false`; amount-conversion + rejection cases). The screen itself is verified via typecheck/lint/build (repo has no component-test infra); the **end-to-end Testnet demo** (supply → see position → withdraw) is the remaining #21 acceptance item — it needs a human driving the wallet with a USDC/XLM-funded account, since a real successful `submit` simulation requires an on-chain token balance. (part of #21)
- 2026-07-07 — Blend: **pool `submit` supply/withdraw call-shape encoder** (`core/blend/pool.ts`, part of #21). The first Blend-specific surface on top of the now-complete Soroban invoke pipeline (`prepareInvoke` #65): `buildBlendSubmitXdr({ poolId, userAddress, reserveAssetId, amount, action, sourceSequence, networkPassphrase, fee? })` turns a "supply N of asset X to earn yield" / "withdraw N of asset X" intent into the **pre-simulation** `invokeHostFunction` XDR for Blend's pool `submit(from, spender, to, requests)` entrypoint, ready to feed `simulateTransaction` (#56) → `assembleInvokeXdr` (#64) → sign. **Why it can't reuse the scalar `InvokeArg` path (#61):** `submit` takes a `Vec<Request>` where each `Request` is a struct `{ address: Address, amount: i128, request_type: u32 }` — a **nested** ScVal (a vec of maps) the flat scalar arg schema can't express — so this module builds that ScVal shape directly, then hands off to the same simulate/assemble tail as every other invoke. The `Request` map keys are emitted in ascending order (`address` < `amount` < `request_type`) as the host requires; `from = spender = to = userAddress` (self-service wallet supply/withdraw, also the tx source). Exposes the full `BLEND_REQUEST_TYPE` enum (Supply=0 … DeleteLiquidationAuction=9, per blend-contracts-v2 `RequestType::from_u32`); the yield MVP maps `supply → Supply` (plain, non-collateral, freely withdrawable — borrowing/collateral is out of scope for #21) and `withdraw → Withdraw`. Validates the pool + reserve-asset contract ids (C…), the user account (G…), and a positive in-range i128 amount. Pure/offline, unit-tested (decoder round-trip → `submit` on the pool; from/spender/to = user; supply vs withdraw `request_type`; ascending map-key order; full i128 precision; bad pool/asset/account/amount rejections). **Remaining for #21:** wire this through the mini-app invoke bridge (typed `InvokeIntent`) + a supply/withdraw UI with a curated Blend testnet pool/asset directory, and a live Soroban RPC round-trip. (part of #21)
- 2026-07-07 — Blend: **curated testnet pool directory** (`core/blend/directory.ts`, part of #21). A small, vetted list (mirroring the anchor directory #54) of Blend lending pools + the reserve assets you can supply into them: `BlendPool`/`BlendReserve` types, `BLEND_POOLS`, `findBlendPool`, `blendPoolsForNetwork`, and `findReserve` (by asset code, case-insensitive). Unlike anchors (discovered from a stellar.toml), pools are addressed directly by contract id, so the ids are pinned here. Seeded with the Blend V2 testnet pool (`CCEBVDYM…`) supporting **USDC** and **XLM** (7 decimals each) — **validated live against `soroban-testnet.stellar.org`**: `get_reserve` simulates OK for USDC/XLM/wETH (BLND is not a reserve), and a `submit` supply built by `buildBlendSubmitXdr` (#67) is accepted and decoded by the pool contract (failing only downstream on the caller's token balance — proving the address + Request ScVal wire-format are correct). Testnet deployments are periodically reset, so the entry documents that it must be re-validated before it's relied on. Pure/offline data module, unit-tested (well-formed pool/reserve entries, valid C… contract ids, no duplicate reserve codes, verified testnet pool lists USDC+XLM, lookup hit/miss, network filter, case-insensitive reserve lookup). Next: a supply/withdraw UI wiring this directory to `buildBlendSubmitXdr` → simulate/assemble → scan → sign. (part of #21)
- 2026-07-07 — Blend: **pool `submit` supply/withdraw call-shape encoder** (`core/blend/pool.ts`, part of #21). The first Blend-specific surface on top of the now-complete Soroban invoke pipeline (`prepareInvoke` #65): `buildBlendSubmitXdr({ poolId, userAddress, reserveAssetId, amount, action, sourceSequence, networkPassphrase, fee? })` turns a "supply N of asset X to earn yield" / "withdraw N of asset X" intent into the **pre-simulation** `invokeHostFunction` XDR for Blend's pool `submit(from, spender, to, requests)` entrypoint, ready to feed `simulateTransaction` (#56) → `assembleInvokeXdr` (#64) → sign. **Why it can't reuse the scalar `InvokeArg` path (#61):** `submit` takes a `Vec<Request>` where each `Request` is a struct `{ address: Address, amount: i128, request_type: u32 }` — a **nested** ScVal (a vec of maps) the flat scalar arg schema can't express — so this module builds that ScVal shape directly, then hands off to the same simulate/assemble tail as every other invoke. The `Request` map keys are emitted in ascending order (`address` < `amount` < `request_type`) as the host requires; `from = spender = to = userAddress` (self-service wallet supply/withdraw, also the tx source). Exposes the full `BLEND_REQUEST_TYPE` enum (Supply=0 … DeleteLiquidationAuction=9, per blend-contracts-v2 `RequestType::from_u32`); the yield MVP maps `supply → Supply` (plain, non-collateral, freely withdrawable — borrowing/collateral is out of scope for #21) and `withdraw → Withdraw`. Validates the pool + reserve-asset contract ids (C…), the user account (G…), and a positive in-range i128 amount. Pure/offline, unit-tested (decoder round-trip → `submit` on the pool; from/spender/to = user; supply vs withdraw `request_type`; ascending map-key order; full i128 precision; bad pool/asset/account/amount rejections). **Remaining for #21:** wire this through the mini-app invoke bridge (typed `InvokeIntent`) + a supply/withdraw UI with a curated Blend testnet pool/asset directory, and a live Soroban RPC round-trip. (part of #21)
- 2026-07-04 — Stellar: **`prepareInvoke` — full build→simulate→assemble pipeline** (`core/stellar/invoke.ts` + `sorobanRpcUrl` on `NetworkConfig`, part of #21). Ties the Soroban invoke primitives into one call the mini-app bridge / a UI uses to turn a typed contract call into a **ready-to-sign XDR**: `prepareInvoke({ sourceAccount, sourceSequence, contractId, functionName, args, networkPassphrase, rpcUrl, fetchImpl?, inclusionFee? })` builds the pre-sim tx (#61) → simulates it against a Soroban RPC (#56) → assembles footprint + auth + resource fee (#64), returning `{ ok: true, xdr }` ready for the existing **scan → approve → SIGN_AND_SUBMIT** path. Every failure mode (bad arg, RPC/network error, contract would revert, missing footprint) comes back as `{ ok: false, error }` instead of throwing, so the caller surfaces one thing. Args are **typed** (`InvokeArg`) — the consumer resolves the intent's arg types. `NetworkConfig` gains an optional `sorobanRpcUrl` (set for Testnet = `soroban-testnet.stellar.org`; Mainnet left unset — no single canonical public RPC). Injectable `fetch`, unit-tested offline (happy path → decodes to the call + fee = inclusion+resource; contract-revert → `ok:false`; a bad arg returns `ok:false` **without** calling the RPC; a non-2xx RPC → `ok:false`). **Remaining for #21: the consumer** — extend the mini-app invoke bridge (typed `InvokeIntent`) or a UI to call `prepareInvoke` → scan → sign — plus a **Blend supply/withdraw surface** (needs real Blend pool contract IDs) and a **live Soroban RPC round-trip** to validate against real simulation output. (part of #21)
- 2026-07-04 — Anchor UI: **Cash in / Cash out — interactive transfer wired end-to-end** (`popup/screens/CashInOut.tsx` + new `popup/components/SandboxedFrame.tsx`, part of #24). Completes the on-ramp flow: picking a **Cash in**/**Cash out** direction now runs SEP-10 auth (`authenticateSep10` #59 — the challenge is validated against the anchor's SEP-1 `SIGNING_KEY`/domains **before** signing, and signed via `SIGN_ONLY` #33, never submitted), starts the SEP-24 interactive session (`startInteractive` #40), and hosts the anchor's hosted deposit/withdraw window in a new **bridge-less sandboxed iframe** (`SandboxedFrame`) with a live status strip driven by `pollTransferStatus` (#62) → `describeTransferStatus` (#55), settling on a terminal state with a **Done** action. `SandboxedFrame` reuses the mini-app Browser's sandboxed-iframe surface (opaque-origin sandbox + framing-blocked "open in new tab" fallback + safe-area overlay) **without** the wallet connect/sign postMessage bridge — a regulated anchor's page has no business talking to it. Completed deposits/withdrawals are real Horizon payments, so they surface in Activity + the balance on return with no extra wiring; and the only thing ever signed is the validated SEP-10 challenge (no anchor-driven arbitrary signing), satisfying "scanned/validated before signing". New pure `webAuthDomainFor()` (host of the WEB_AUTH_ENDPOINT, home-domain fallback), unit-tested. Screen verified via typecheck/lint/build; the end-to-end **Testnet demo against a live anchor** is the remaining acceptance item (needs a real anchor round-trip + KYC, not automatable here). (part of #24)
- 2026-07-04 — Stellar: **Soroban invoke `assemble` step + auth capture** (`assembleInvokeXdr` in `core/stellar/invoke.ts`, `auth` in `core/stellar/soroban.ts`, part of #21). Closes the gap between the invoke **builder** (#61) and signing: `simulateTransaction` (#56) now also surfaces the `auth` entries a call requires (from the simulation's `results[0].auth`), and `assembleInvokeXdr({ builtXdr, minResourceFee, transactionData, auth?, inclusionFee? })` applies a simulation result to a pre-simulation invoke tx to produce the **ready-to-sign** XDR: it attaches the simulated **footprint** (`SorobanTransactionData`) and **auth entries** to the single `invokeHostFunction` op and sets the total fee to `inclusion + minResourceFee` (a Soroban tx pays a classic inclusion fee **plus** the resource fee), preserving source/sequence/timebounds; rejects a fee-bump, a non-invoke or multi-op tx, and a missing footprint. Pure/offline. Unit-tested against SDK-constructed fixtures (footprint attached — tx ext arm 1; fee = 100+resource and a custom inclusion fee; call survives the rebuild; no-footprint throws) + simulate's auth capture (present / empty-omitted). **The build→simulate→assemble pipeline is now complete; wiring it to the signing path + a Blend supply/withdraw UI (needs real Blend pool contract IDs) is the next slice.** NOTE: the assemble inputs come from a live Soroban RPC — this is unit-tested against fixtures but only truly validated by a real RPC round-trip. (part of #21)
- 2026-07-04 — Anchor: **SEP-24 transaction status poller** (`core/anchor/poll.ts`, part of #24). The polling half of acceptance item 3: after `startInteractive` (#40) returns a transaction id, `pollTransferStatus()` polls `GET /transaction` (#40) until the deposit/withdraw reaches a terminal state, mapping each status through `describeTransferStatus` (#55). Pure and injectable — the `fetch` and the inter-poll `wait` are both injected — so it's fully unit-tested offline with no real timers. `onUpdate` fires only on an actual status *change* (so a UI subscription isn't spammed with identical `pending_anchor` ticks); terminal is decided by `describeTransferStatus(...).terminal`, so an unknown/future anchor status keeps polling rather than stopping early; a first-fetch failure throws while a later transient failure is swallowed and retried; `isCancelled()` stops the loop (e.g. screen unmount) and `maxAttempts` bounds it. Unit-tested (drive-to-terminal, change-only onUpdate, error-terminal, unknown-status keeps polling, first-fetch throw, transient-retry, cancel, maxAttempts). Wiring it into the Cash-in/out screen + folding completed transfers into Activity is the next slice. (part of #24)
- 2026-07-04 — Anchor: **SEP-10 auth → SEP-24 interactive orchestrator** (`core/anchor/session.ts`, part of #24). Ties the anchor transfer flow together as a pure, injectable orchestrator so the security-critical sequencing is unit-tested rather than buried in UI: `authenticateSep10()` fetches the SEP-10 challenge (#39), **validates it against the anchor's SEP-1 `SIGNING_KEY` + expected home/web-auth domains + network BEFORE signing**, and only a validated challenge is handed to the injected `signChallenge` (the app wraps `SIGN_ONLY` #33), then submits it for a JWT. `startAnchorTransfer()` chains that into `startInteractive` (#40), returning the interactive URL (for the `Browser` overlay) + tx id (to poll). The signer + `fetch` are injected, so it's fully offline-testable — including the guard tests that a challenge signed by the **wrong key** and one built for a **different network** are refused and the signer is **never called**. The Cash-in/out screen wiring (a `SIGN_ONLY`-backed signer + opening the interactive URL in the existing sandboxed `Browser` overlay) + status polling/Activity are the next slice. (part of #24)
- 2026-07-04 — Receive UI: **"Receive" address-QR screen** (`popup/screens/Receive.tsx` + pure `core/receive/payload.ts`, part of #19). New `qr_code_2` AppBar action opens a full-screen overlay (same pattern as Guardians/CashInOut, `receiveOpen` in `App.tsx`) that renders the wallet address as a scannable QR via the small well-known `qrcode` dep (`QRCode.toDataURL` → `<img>`), shows the full address in mono below it, and offers a "Copy address" button reusing the existing `useToast()` → "Address copied". The QR payload is factored into a pure `receiveQrPayload(address)` helper — raw account key is the receive standard; it validates via `StrKey.isValidEd25519PublicKey` and rejects non-keys/secrets so a secret can never be encoded — unit-tested (raw payload, whitespace-trim, invalid/secret/empty rejection). A SEP-0007 `web+stellar:pay` request URI is a deliberate follow-up. The camera-SCAN half (for Send) is out of scope here — it needs a native-camera-permission decision. Screen verified via typecheck/lint/build (repo has no component-test infra). (part of #19)
- 2026-07-04 — Stellar: **typed Soroban invoke-contract XDR builder** (`core/stellar/invoke.ts`, part of #21). The "invoke builder" the Blend integration needs — the construction counterpart to the merged `simulateTransaction` wrapper (#56), and the typed ScVal step the mini-app `InvokeIntent` (`core/miniapps/bridge.ts`) explicitly deferred (it carries args as bare strings). A typed-arg schema `InvokeArg = { type; value: string }` covers `address | symbol | string | bool | u32 | i32 | u64 | i64 | u128 | i128 | bytes`; `argToScVal(arg)` converts one arg via the SDK (`Address(...).toScVal()` for addresses accepting both G…/C…; `nativeToScVal` for 32-bit ints and symbols; `XdrLargeInt` for 64/128-bit ints at full precision; `scvString`/`scvBool`/`scvBytes` otherwise) and throws a clear Error on a bad value — non-numeric or out-of-range int (BigInt range-checked, since `nativeToScVal` silently wraps), invalid address, over-long/illegal symbol, odd-length or non-hex bytes. `buildInvokeContractXdr({ sourceAccount, sourceSequence, contractId, functionName, args, networkPassphrase, fee? })` validates the contract id (C…) and function name (Soroban symbol, ≤32) then builds a single-op `InvokeHostFunction` transaction via `new Contract(id).call(fn, ...scVals)` + `setTimeout` and returns `.build().toXDR()`. **Scope: this is the PRE-simulation tx** — resource fees / footprint come from `simulateTransaction` (#56); applying that estimate (assemble) + wiring to signing is a follow-up. Pure builder, no network, no new deps. Unit-tested: build→`decodeTransaction` round-trip (contractId + function match), per-type `scValToNative(argToScVal(...))` value recovery, and rejection of bad contract id / function name / typed values. (part of #21)- 2026-07-04 — Stellar: **Soroban RPC `simulateTransaction` wrapper** (`core/stellar/soroban.ts`, part of #21). The "RPC-simulated invoke" primitive the Blend integration needs — the simulation half deferred from the invoke-intent work (#36). `simulateTransaction(txXdr, { rpcUrl, fetchImpl? })` POSTs a JSON-RPC 2.0 `simulateTransaction` to a Soroban RPC node and normalizes the reply into a discriminated union: success → `{ ok: true, minResourceFee, transactionData?, latestLedger? }`; the contract would revert (`result.error` set) or a JSON-RPC top-level `error` → `{ ok: false, error }`; a non-2xx HTTP response throws with the status (mirrors the SEP clients); a malformed/unreadable body degrades to `{ ok: false, error: 'Unreadable simulation response.' }`. The INPUT is an already-built transaction envelope XDR — this module builds no ScVals or invoke op. The transport is an injectable `fetch` (not `rpc.Server`), so the whole path is unit-tested offline (success parse, contract-failure, JSON-RPC error, non-2xx throw, malformed body). Ships a default `SOROBAN_TESTNET_RPC` constant while the function still takes `rpcUrl` explicitly. Feeds the eventual RPC-simulated invoke builder. (part of #21)
- 2026-07-04 — Scan: **plain-language labels for DeFi contract calls** (`core/scan/defi.ts` + the Soroban branch of `core/scan/explainer.ts`, part of #21). Builds on decode exposing `contractFunction`/`contractId` (#34) and the plain-language setOptions work (#37) — the same "spell it out before signing" goal. A curated map turns well-known DeFi/lending function names into a short human phrase (`supply`/`deposit` → "Deposits funds into a lending pool", `withdraw` → "Withdraws funds from a lending pool", `borrow` → "Borrows against your collateral", `repay` → "Repays borrowed funds", `claim`/`claim_rewards` → "Claims rewards"), matched case-insensitively on the decoded Soroban symbol. When recognized, the approval sentence leads with the action and still names the raw function + contract (e.g. `Deposits funds into a lending pool — calls "supply" on a smart contract (CA3D…GAXE). Only continue if you trust it.`); unrecognized functions and non-invoke Soroban ops (upload-wasm / create-contract) keep the existing generic wording. Pure, wording-only — no new deps, no network, no change to decode/types or scan verdicts. Unit-tested (recognized phrase + still names the fn, case-insensitivity, unrecognized regression guard, no-function fallback). (part of #21)
- 2026-07-04 — Anchor UI: **"Cash in / Cash out" anchor-discovery screen** (`popup/screens/CashInOut.tsx` + `core/anchor/transfer.ts`, part of #24 item 4). First user-facing surface for the fiat ramp — a new `currency_exchange` AppBar action opens a full-screen overlay (same pattern as Guardians/Scan) that lists the curated anchors for the current network (`anchorsForNetwork` #54), and on selecting one runs SEP-1 discovery (`discoverAnchor` #38) → SEP-24 `GET /info` (`fetchSep24Info` #52) to show which assets you can **cash in** (deposit) or **cash out** (withdraw), with per-asset limits. A new pure `summarizeAssetSupport()` merges the `/info` deposit+withdraw maps into one row per asset (a side counts only if listed **and** enabled; assets with nothing usable are dropped) and `formatTransferLimits()` renders min/max — both unit-tested. Picking a direction advances to a handoff step describing the next step (sign a one-time SEP-10 auth challenge — never submitted — then complete in the anchor's secure window, with any signature scanned first); the actual SEP-10 auth + SEP-24 interactive (in the existing `Browser` overlay) + status polling / Activity integration are the next slice. Screen verified via typecheck/lint/build; not yet runtime-verified in-browser (needs the Testnet anchor round-trip). (part of #24)
- 2026-07-04 — Anchor: **SEP-24 `/info` asset discovery** (`fetchSep24Info` in `core/anchor/sep24.ts`, part of #24). Completes the SEP-24 client (alongside `startInteractive` + `fetchTransaction` #40): `GET /info` on the anchor's `TRANSFER_SERVER_SEP0024` returns which assets support deposit/withdraw plus per-asset limits (`min_amount`/`max_amount`) and fees (`fee_fixed`/`fee_percent`), so a "Cash in / Cash out" screen can list the supported assets (e.g. XLM/`native`, USDC) **before** the user authenticates — `/info` is a public endpoint, so no SEP-10 JWT is attached. Assets are returned sorted by code with `enabled` preserved (SEP-24's omit-means-true honored; only an explicit `false` disables) so the UI can grey out disabled ones; numeric fields are defensively coerced and a response missing `deposit`/`withdraw` degrades to empty lists. Injectable `fetch`, unit-tested offline (maps limits/fees, enabled defaulting, trailing-slash + missing-sides tolerance, non-2xx throws). Satisfies the #24 "SEP-24 `/info`" acceptance item; the anchor directory + Cash-in/out UI remain. (part of #24)
- 2026-07-04 — Anchor: **curated anchor directory** (`core/anchor/directory.ts`, part of #24 item 4). A small, vetted list of anchors for the Cash in / Cash out demo — verified/vetted entries, not an open free-for-all — mirroring the existing curated `core/miniapps/directory.ts` (`AnchorEntry`/`ANCHORS`/`findAnchor`, plus `anchorsForNetwork` filtering and an `isValidHomeDomain` guard). Seeded with the SDF reference SEP-24 testnet anchor (`testanchor.stellar.org`, SRT + USDC). Pure/offline: entries carry the SEP-1 `homeDomain` that `discoverAnchor` fetches `.well-known/stellar.toml` from — no network calls here. Unit-tested (seed well-formed, lookup hit/miss, network filter, domain guard). (part of #24)
- 2026-07-04 — Anchor: pure **SEP-24 transfer-status labels** (`src/core/anchor/status.ts`) — `describeTransferStatus` maps each `GET /transaction` status to a plain-language label, a `kind` (pending / action-needed / done / error), and a `terminal` flag, with a safe fallback for unknown anchor values. Feeds the upcoming status-polling + Activity surfacing. (part of #24)
- 2026-07-04 — Recovery UI: **recover-an-account flow** (`RecoverAccount.tsx`, part of #23 M1 item 4 — completes the recovery flow end-to-end). The recovering device's side: from a "Lost access to another account? Recover it" link on the Guardians screen, enter the account you lost, and it reads that account's guardians (`loadAccountSigners` + `classifyGuardianConfig` #45), builds the recovery request that adds **this** device's key (`buildRecoveryXdr` #42), shows it to share with the guardians, collects their pasted-back signed copies (`mergeGuardianSignatures` #47 — mismatched copies rejected), tracks weight collected vs the account's threshold (`collectedSignatureWeight` #43), and once enough are gathered broadcasts the merged tx with `SUBMIT_ONLY` (#50 — no self-signature, so no `tx_bad_auth_extra`). Thin orchestrator over already-tested primitives. (#51, part of #23)
- 2026-07-04 — Session: **`SUBMIT_ONLY` broadcast-an-already-signed-tx primitive** (`core/session/handler.ts`, part of #23 M1 item 4). New worker message that submits a fully-signed transaction to Horizon **without adding a signature**. The recovering device needs this to broadcast the guardian-signed recovery tx: its own key is **not** a signer on the account being recovered, so `SIGN_AND_SUBMIT` would attach an unused signature and Horizon would reject the whole tx with `tx_bad_auth_extra`. Unlike the signing paths it needs **no unlocked session** (it only broadcasts, never touches the key). Reachable on extension + native via the generic worker forward; unit-tested (not session-gated; malformed XDR fails gracefully, never `LOCKED`). Unblocks the recovering-device submit step. (#50, part of #23)
- 2026-07-04 — Recovery UI: **guardian co-sign screen** (`CoSignRecovery.tsx`, part of #23 M1 item 4). The guardian side of social recovery: reached from a "Helping someone recover? Co-sign their request" link on the Guardians screen, you paste the recovery request someone shared, review exactly what it changes (existing decode/scan/explain #31/#37 → `RiskCallout` → CONFIRM/press-and-hold gate), co-sign it with `SIGN_ONLY` (#33 — signed, never submitted here), and copy the signed request back. **Safe by construction:** a new pure `recoveryCoSignError()` guard refuses to sign unless the request (a) is readable, (b) does **not** modify the guardian's *own* account — a self-sourced tx co-signed with the guardian's master key would be an account **takeover** of the guardian, so `source === ownAddress` is rejected — and (c) is a pure recovery (every op a setOptions signer/threshold change, via `isRecoveryTransaction`), so a payment (or a mixed tx) can't be dressed up as a "recovery." It's not a generic sign-any-XDR oracle. Guard unit-tested incl. the takeover case (accepts an other-account recovery; rejects a self-sourced setOptions, a payment, and unreadable input). (#49, part of #23)
- 2026-07-04 — Recovery UI: **manage guardians (edit / add / remove)** on the Guardians screen (`Guardians.tsx`, part of #23 M1). Lifts the re-setup block from #45 by routing edits through the safe `buildGuardianUpdateXdr` (#46). When an account already has recovery, the screen becomes an editor: it prefills the guardian list + threshold from the account's current signers, and on submit builds an *update* tx (weight-0 removals + adds + thresholds recomputed from the final set — anti-drain invariant preserved) instead of a first-time setup. The review screen shows an added/removed diff (new pure `guardianDiff()` helper, unit-tested) and routes through the same high-risk scan → confirm gate; copy adapts ("Manage Guardians" / "Update Recovery"). First-time setup on a fresh account is unchanged. (#48, part of #23)
- 2026-07-03 — Recovery: **`mergeGuardianSignatures` — combine out-of-band co-signatures** (`core/recovery/guardians.ts`, part of #23 M1 item 4). The recovery-coordination flow shares one recovery XDR to each guardian, who signs a copy on their own device via `SIGN_ONLY` (#33); this merges all their signatures back onto the base tx ready to submit. Security-critical guard: every signed copy must be the **same transaction** (matching tx hash) as the base — a copy for a *different* tx is rejected, so a malicious guardian can't get their signature counted toward a transaction they swapped in. Duplicate signatures are ignored. Pure/offline; composes with the co-signature tally (#43) to know when enough weight is collected. Unit-tested (combine two guardians, dedup, reject-different-tx, empty). (#47, part of #23)
- 2026-07-03 — Recovery: **`buildGuardianUpdateXdr` — safe add/replace/remove of guardians** (`core/recovery/guardians.ts`, part of #23 M1). The setup builder only *adds* signers, so it can't safely change an existing guardian set (#45 blocks re-setup for that reason). This new builder transitions `currentGuardians → desiredGuardians`: emits weight-0 **removals** for dropped guardians, adds new ones, and recomputes the owner weight + thresholds from the **final** set (`ownerWeight = desired+1`, `high = K`) — so the anti-drain invariant ("all guardians combined < the medium threshold") holds *after* the change, not just at first setup. Precondition: caller supplies the account's complete on-chain guardian set (from `classifyGuardianConfig`). Shared guardian validation refactored out of the setup builder. Unit-tested incl. the invariant (grow to 4 → medium threshold 5 > 4). Wiring this into a manage-guardians UI (to unblock #45's re-setup) is the follow-up. (#46, part of #23)
- 2026-07-03 — Recovery UI: **current-guardians view + block unsafe re-setup** on the Set Up Recovery screen (`Guardians.tsx`, part of #23 M1). The screen didn't show an account's existing guardians, and — because `buildGuardianSetupXdr` only *adds* signers (setOptions never removes the others) while recomputing thresholds for the newly-entered count — re-running setup on a configured account would **accumulate** guardians and silently break the anti-drain threshold invariant. Now, on open, it reads the account's signers via a new `loadAccountSigners()` and a pure `classifyGuardianConfig()` (guardians = non-master signers; recovery threshold = the account's high threshold); if recovery is already on it shows a read-only "Recovery is on — any K of N can recover" card listing the guardians and **blocks re-setup** (form + Review hidden, `toReview` guarded) with an accurate "changing guardians isn't supported yet" note, since the builder is first-time-setup-only. Classifier unit-tested. (#45, part of #23)
- 2026-07-03 — Recovery UI: **"Set Up Recovery" guardian screen** (`src/popup/screens/Guardians.tsx`, #23 M1 item 3). First user-facing surface for guardian social recovery — reached from a new `shield_person` AppBar action, opened as a full-screen overlay (same pattern as the Security/Scan screen). Add guardian addresses, pick a K-of-N recovery threshold, then it builds the setup tx via `buildGuardianSetupXdr` (#41), runs it through the existing Lantern pre-sign **scan → RiskCallout → CONFIRM/press-and-hold gate** (high-risk account-control change, #31/#37), signs & submits, and shows a success + explorer link. Mirrors the Send flow; reuses `buildGuardianSetupXdr`'s validation for guardian keys/threshold. New pure `describeGuardianSetup()` renders the plain-language quorum summary ("Any 2 of your 3 guardians can help you recover…"), unit-tested. The review screen shows the **true total** network fee via a new `totalFeeXlm()` helper — the setup is a multi-op tx so the fee is `baseFee × (N+1)`, not `baseFee` — regression-tested. (#44, part of #23)
- 2026-07-03 — Recovery: **guardian co-signature tally** (`collectedSignatureWeight` / `hasThresholdSignatures` in `core/recovery/guardians.ts`). The last non-UI piece of the recovery coordination flow (#23 M1, item 4): as guardians co-sign a recovery tx out-of-band via `SIGN_ONLY` (#33), these compute how much signature weight has been collected (via the SDK's `WebAuth.gatherTxSigners`, summing only the account's known signers) and whether it has reached the account's high threshold K — i.e. when it's safe to submit. Only listed signers count, so a stray signature can't inflate the tally. Pure/offline; unit-tested (0/1/2-of-3 tallies, per-signer weights, unknown-signature rejection, threshold reached at K). (#43, part of #23)
- 2026-07-03 — Recovery: **`buildRecoveryXdr` — install-new-key recovery transaction** (`core/recovery/guardians.ts`). Completes the transaction-building side of guardian recovery (#23 M1, item 4): when the owner key is lost, the fresh device drafts one `setOptions` that installs its new key at owner-level weight (`N+1`, mirroring the setup so it operates normally) and disables the lost master (`masterWeight 0`). It's a HIGH-threshold change, so under the `highThreshold = K` the setup installed it's authorized by exactly K guardian co-signatures (each collected out-of-band via `SIGN_ONLY` #33). Thresholds + existing guardian signers are left untouched, so the account stays guardian-protected. Validates the new key, rejects self. Also tightens `buildGuardianSetupXdr`'s guardian cap from 20 → **19** (`MAX_SIGNERS − 1`) so a recovery signer slot is always reserved — otherwise an `N=20` setup would produce a permanently unrecoverable account within Stellar's 20-signer limit. Unit-tested via the scan decoder (#31) + confirmed scanned high-risk and explained as *"adds signer … removes your own key's signing power"* (#31/#37), plus the 19-guardian headroom boundary. The Guardians & Recovery UI + QR/paste co-sign hand-off remain the rest of #23. (#42, part of #23)
- 2026-07-03 — Recovery: **`buildGuardianSetupXdr` guardian multi-sig setup** (`core/recovery/guardians.ts`). Composes the setOptions primitive (#35) into the actual weighted-multisig setup transaction for guardian social recovery (#23 M1, items 3–4): adds each guardian as a weight-1 signer (one op each), then sets the account's weighting. Security-critical policy (decouples owner power from the guardian quorum, since Stellar puts **Payment on the *medium* threshold but signer `setOptions` on the *high* threshold**): owner `masterWeight` + `low`/`medThreshold` = `N+1` (strictly above all guardians combined → guardians can **never** authorize a payment/offer/trustline change), and **only** `highThreshold` = `K` so any K guardians can co-sign the recovery `setOptions` and nothing lower. Owner keeps full solo control, never locked out. Validates distinct valid keys, `1 ≤ K ≤ N ≤ 20`, rejects self-as-guardian. Pure/offline; unit-tested by round-tripping through the scan decoder (#31), asserting the on-chain invariant (K guardians clear high but not medium), and confirming the scanner flags it high-risk `block_confirm` (#31/#37). The Guardians & Recovery UI + QR/paste co-sign hand-off remain the rest of #23. (#41, part of #23)
- 2026-07-03 — Anchor: **SEP-24 interactive transfer client** (`core/anchor/sep24.ts`). The transfer step after SEP-10 auth (#24): `startInteractive(transferServer, {kind, assetCode, account, jwt})` POSTs a deposit/withdraw to `TRANSFER_SERVER_SEP0024/transactions/{deposit|withdraw}/interactive` (bearing the SEP-10 JWT) and returns the interactive `url` (to open in the mini-app Browser overlay) + transaction `id`; `fetchTransaction` polls `/transaction?id=` and maps status/amounts. Injectable `fetch`, unit-tested offline. (#40, part of #24)
- 2026-07-03 — Anchor: **SEP-10 web authentication client** (`core/anchor/sep10.ts`). The auth step between SEP-1 discovery (#38) and any SEP-24 transfer (#24): `fetchChallenge` GETs the anchor's challenge from `WEB_AUTH_ENDPOINT`; `validateChallenge` wraps the SDK's `WebAuth.readChallengeTx` to verify the challenge is server-signed by the SEP-1 `SIGNING_KEY`, sequence 0, in-timebounds, and for the right home/web-auth domain — the guard that makes handing an anchor transaction to `SIGN_ONLY` (#33) safe; `submitChallenge` POSTs the signed challenge back for the SEP-10 JWT. Network calls take an injectable `fetch`, so the whole flow (incl. real challenge fixtures via `WebAuth.buildChallengeTx`) is unit-tested offline. (#39, part of #24)
- 2026-07-03 — Anchor: **SEP-1 `stellar.toml` discovery** (`core/anchor/toml.ts`). First step of the "Cash in / Cash out" flow (#24): `parseAnchorToml(text)` extracts the SEP-10 `WEB_AUTH_ENDPOINT` + `SIGNING_KEY` (validated as a real account key, dropped if not) and the SEP-24 `TRANSFER_SERVER_SEP0024`, plus `[[CURRENCIES]]`. `discoverAnchor(homeDomain, fetchImpl?)` fetches `https://<domain>/.well-known/stellar.toml` (scheme/trailing-slash tolerant) and parses it; `fetchImpl` is injectable so the whole path is unit-tested offline. Adds `toml@3.0.0` (MIT) as a **new** production dependency — the SDK exposes only a network `StellarToml.Resolver` (no pure string parse), and since the 15→16 bump (#32) it no longer bundles a standalone `toml` parser (it moved to `smol-toml` internally, not re-exported) — so we pull in a small, well-known MIT parser to keep SEP-1 discovery pure and offline-testable. (#38, part of #24)
- 2026-07-03 — Scan: **spell out signer/threshold changes in plain language**. The `setOptions` explanation was a single generic sentence; it now names exactly what changes — *"This adds signer GDVE…ZA57 with weight 1, and sets the signing thresholds (low 1, medium 2, high 2)"*, *"removes signer …"* (weight 0), *"removes your own key's signing power"* (masterWeight 0) — from the fields already decoded in #31. Directly satisfies the #23 acceptance criterion ("explains signer/threshold changes in plain language"); the highest-stakes op type to get right, since a signer change is how an account gets taken over. Pure, unit-tested. (#37, part of #23)
- 2026-07-03 — Mini-app bridge: **contract-invocation intent shape**. `core/miniapps/bridge.ts` could only model a classic `PaymentIntent`; add `validateInvokeIntent` (+ `InvokeIntent` type and a `isValidContractId` helper in `wallet/`) so a DeFi mini-app (e.g. Blend `supply`) can request a Soroban contract call through the same connect → scan → approve → sign pipeline as payments. Validates the `C…` contract address, the function name (Soroban symbol: alnum/underscore, ≤32), and normalizes positional args to strings (typed ScVal construction is deferred to the RPC-simulated invoke builder). Pure, unit-tested; pairs with the contract-call scan decode (#34). (#36, part of #21)
- 2026-07-03 — Stellar: **`buildSetOptionsXdr` weighted-multisig builder**. New pure tx builder for the social-recovery primitive (#23): add/remove a guardian signer (weight 0 removes) and/or set low/med/high thresholds + `masterWeight`. Emits only the fields actually being changed (an unspecified threshold is never silently zeroed), validates weights 0–255, and throws on an empty (no-op) change. Completes the #23 M1 primitive set alongside the setOptions scan-decode (#31) and the `SIGN_ONLY` co-signing path (#33); unit-tested by round-tripping the built XDR back through the scan decoder. (#35, part of #23)
- 2026-07-02 — Scan: **name the Soroban contract call**. `decode.ts` previously only set an `isSoroban` boolean, so every contract invocation scanned as a generic "Smart contract call". Now it decodes the `invokeHostFunction` host function into `contractId` + `contractFunction`, and the scan reason + explainer name what's being called (e.g. *"This calls 'supply' on contract CA3D…GAXE"*). Upload-wasm / create-contract host functions (no callable fn) still decode safely and stay flagged as Soroban. Pure decode, defensive parsing, unit-tested; the "Soroban contract scanning" groundwork the Blend integration needs. (#34, part of #21)
- 2026-07-02 — Session: **`SIGN_ONLY` sign-but-don't-submit primitive**. New worker message that signs a transaction with the in-memory keypair and returns the signed XDR **without** submitting to Horizon (mirrors `SIGN_AND_SUBMIT`, stops before broadcast). Because `fromXDR` preserves existing signatures and `sign()` appends, it doubles as a **co-signing** primitive — a guardian adds their signature to a partially-signed recovery tx, and SEP-10 signs a challenge tx that's returned, never submitted. Locked-session gated; unit-tested (signature verifies against the account, and a second call appends a 2nd signature). Unblocks anchor SEP-10 auth (#24). (#33, part of #23)
- 2026-07-02 — Security/deps: **`@stellar/stellar-sdk` 15.1.0 → 16.0.1** to clear the transitive `axios` advisories (bumps `axios` 1.15.x → 1.16.1, above the vulnerable range — prototype-pollution/credential-leak class). `npm audit --omit=dev` now reports 0 production vulnerabilities. Regression pass on the SDK surface (tx builders, Horizon client, session handler, scan decode): typecheck, lint, all 75 unit tests, and the production build pass unchanged — no source changes needed. (The one remaining dev-only `vite` advisory is not shipped and is left out of scope.) (#32, part of #18)
- 2026-07-02 — Scan: **flag signer/threshold changes before signing**. The scanner now decodes `setOptions` (signers, `masterWeight`, low/med/high thresholds) instead of letting it fall through as an unrecognized op, and raises a high-risk `account_control_change` verdict (`block_confirm`) — with a stronger "gives up account control" message when `masterWeight` is set to 0. Previously a signer/threshold change scanned as low-risk `allow`. Pure decode + engine rule, unit-tested; first consumer of the account-control decode groundwork for guardian recovery. (#31, part of #23)
- 2026-07-02 — Native: **live settings propagation**. `onSettingsChanged` was a no-op on native, leaving non-active surfaces stale after e.g. a network toggle; add an in-process subscriber set that `setSettings` fans out to on native (matching the extension's `chrome.storage.onChanged` behavior), unit-tested. (#30, part of #19)
- 2026-07-02 — Mobile: **press-and-hold to confirm high-risk signs**. Replaces the typed-`CONFIRM` gate on native (Send + mini-app Apps approval) with a reusable `HoldToConfirm` button that fills a progress track and fires once — no on-screen keyboard, same "no accidental tap" guarantee. Extension keeps typed-`CONFIRM`. (#29, part of #19)
- 2026-07-02 — UX: **Send recent-recipients shortcut**. Pure `recentRecipients()` derives distinct, newest-first sent-to addresses from decoded tx history (outgoing payments + account creations; ignores incoming/swap/failed/self), unit-tested; Send form shows up to 3 as tappable chips that refill the destination. (#28, part of #19)
- 2026-07-02 — Android launcher icon: replace Capacitor's placeholder with the real **Lantern amber mark** across all mipmap densities (legacy square, circle-masked round, and adaptive foreground) + navy adaptive background. Generated from the same source art as the extension icons (`logo.jpg`) via a new cross-platform `scripts/gen-android-icons.mjs` (`npm run icons:android`, sharp-based) so the two sets can't silently diverge. (#25, closes #20)
- 2026-07-02 — Security: **Android lifecycle auto-lock**. Backgrounding the app (home/app-switcher) previously left the in-memory session unlocked for the full idle window; add a native `@capacitor/app` `pause` listener (gated on `isNativePlatform`, dynamically imported) that locks immediately on background, complementing the idle timer. (#27, part of #18)
- 2026-07-02 — UX: **copy-address confirmation toast**. Copying the wallet address (AppBar) or the demo-flagged address (Scan) was a silent clipboard write; add a minimal app-wide `ToastProvider`/`useToast` (aria-live) showing "Address copied" on success. (#26, part of #19)
- 2026-07-01 — Android fix: mini-app **browser overlay clears the status bar & gesture bar**. `#root`'s `transform` makes it the containing block for the `fixed` overlay, but a transformed ancestor insets against its *padding box* — so `inset-0` overlapped `#root`'s safe-area padding and ran the header under the status bar. Inset the overlay box itself via `env(safe-area-inset-*)`; extension/desktop unaffected (env → 0). (closes #9)
- 2026-06-30 — Mini-app bridge v2: issued (non-native) **asset payments** + read-only **sign-message** auth (`lantern:signMessage` → domain-separated `SIGN_MESSAGE`), with the intent logic extracted to a unit-tested `core/miniapps/bridge.ts`. Mock dApp gains an asset selector + Sign-message panel. (#13)
- 2026-06-30 — Mini-app `signAndSubmit` bridge: a connected dApp sends a payment **intent**; Lantern builds, AI-scans, has the user approve, signs & submits, returns the tx hash. Richer Get Balance mock dApp (send, all assets, Friendbot fund). (#12)
- 2026-06-30 — Read-only wallet **connect bridge** (`lantern:getPublicKey` → approval → public key + network) + deployable Get Balance mock dApp. (#11, closes #10)
- 2026-06-30 — Android layout: fill the device viewport (`100dvh`) + safe-area insets + status-bar overlay so the UI isn't a fixed 360×600 box. (#8, closes #7)
- 2026-06-30 — Android (Capacitor) milestone 1: storage/messaging seams, in-process session handler, mobile build, committed `android/` project, debug-APK CI. (#6)
- 2026-06-24 — Demo: mock mini-app browser launching blockhub.academy + AI scan on send; investor pitch deck and screenshots. (#3, closes #2)

<!-- auto-dev: prepend new entries above this line -->
