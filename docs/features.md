# Features log

A running, reverse-chronological log of shipped changes. The auto-dev agent
appends an entry here for every change it lands (see `auto-dev.md`).

Format: `- YYYY-MM-DD — <summary> (#PR, closes #issue)`

## Shipped

- 2026-07-07 — Stellar: **path-payment swap builder** (`buildPathPaymentStrictSendXdr` in `core/stellar/swap.ts`, part of #71). First slice of in-wallet swaps: builds the *unsigned* XDR for a market swap via a **strict-send path payment** — send exactly `sendAmount` of `sendAsset`, receive at least `destMin` of `destAsset`, routed through `path` across the native SDEX order book + AMM pools. A swap is a self-payment (destination defaults to the source), and `destMin` is the **slippage floor** the network enforces atomically (a price move can't silently give a worse rate). Validates strictly-positive amounts (≤7dp), distinct send/dest assets, and well-formed asset refs; reuses the existing `AssetRef` shape. Ships `destMinFromQuote(quoted, tolerance)` to compute the floor from a quote. Pure/offline — signing + scan run through the existing worker path; the decoder already recognizes `pathPaymentStrictSend`. Unit-tested (self-swap XLM→USDC round-trip, custom destination, intermediate path, issued→issued, identical-asset/amount/precision/missing-issuer rejections, slippage-floor math). Next: Horizon strict-send path discovery, swap scan-labeling, the Swap UI, and the Soroswap aggregator engine. (part of #71)
- 2026-07-07 — Blend UI: **"Earn yield" supply/withdraw screen, wired end-to-end** (`popup/screens/Earn.tsx` + new `core/blend/submit.ts`, part of #21). The user-facing capstone of the Blend integration: a new `savings` AppBar action opens a full-screen overlay (same pattern as Guardians/CashInOut) that lists the curated Blend pools for the network (`blendPoolsForNetwork` #68) with per-reserve **Supply**/**Withdraw** actions, takes an amount, and runs it through the wallet's existing **scan → approve → SIGN_AND_SUBMIT** path. The core glue is a new pure, unit-tested `prepareBlendSubmit()` — the **build → simulate → assemble** pipeline for a Blend `submit`, mirroring `prepareInvoke` (#65) but for Blend's nested `Request` call shape (which the scalar `InvokeArg` path can't express, so `prepareInvoke` can't be reused): it builds the pre-sim tx (`buildBlendSubmitXdr` #67) → simulates against the network's Soroban RPC (`simulateTransaction` #56, capturing footprint + auth + resource fee) → assembles the ready-to-sign XDR (`assembleInvokeXdr` #64), returning `{ ok, xdr } | { ok:false, error }` so the screen surfaces one thing. A pure `toBaseUnits(human, decimals)` converts the entered amount to the i128 base units the builder needs (rejecting over-precision / non-positive input). The prepared tx is **scanned before signing** (advisory `RiskCallout`/`ScanBadge` + the high-risk CONFIRM / press-and-hold gate reused from Send), and the review sheet spells out the action/pool/asset/amount in plain language. `prepareBlendSubmit` + `toBaseUnits` are unit-tested offline with an injected `fetch` (build→sim→assemble happy path → decodes to `submit` on the pool, fee = inclusion+resource; contract-revert → `ok:false`; bad amount → `ok:false` **without** calling the RPC; non-2xx RPC and missing-footprint → `ok:false`; amount-conversion + rejection cases). The screen itself is verified via typecheck/lint/build (repo has no component-test infra); the **end-to-end Testnet demo** (supply → see position → withdraw) is the remaining #21 acceptance item — it needs a human driving the wallet with a USDC/XLM-funded account, since a real successful `submit` simulation requires an on-chain token balance. (part of #21)
- 2026-07-07 — Blend: **pool `submit` supply/withdraw call-shape encoder** (`core/blend/pool.ts`, part of #21). The first Blend-specific surface on top of the now-complete Soroban invoke pipeline (`prepareInvoke` #65): `buildBlendSubmitXdr({ poolId, userAddress, reserveAssetId, amount, action, sourceSequence, networkPassphrase, fee? })` turns a "supply N of asset X to earn yield" / "withdraw N of asset X" intent into the **pre-simulation** `invokeHostFunction` XDR for Blend's pool `submit(from, spender, to, requests)` entrypoint, ready to feed `simulateTransaction` (#56) → `assembleInvokeXdr` (#64) → sign. **Why it can't reuse the scalar `InvokeArg` path (#61):** `submit` takes a `Vec<Request>` where each `Request` is a struct `{ address: Address, amount: i128, request_type: u32 }` — a **nested** ScVal (a vec of maps) the flat scalar arg schema can't express — so this module builds that ScVal shape directly, then hands off to the same simulate/assemble tail as every other invoke. The `Request` map keys are emitted in ascending order (`address` < `amount` < `request_type`) as the host requires; `from = spender = to = userAddress` (self-service wallet supply/withdraw, also the tx source). Exposes the full `BLEND_REQUEST_TYPE` enum (Supply=0 … DeleteLiquidationAuction=9, per blend-contracts-v2 `RequestType::from_u32`); the yield MVP maps `supply → Supply` (plain, non-collateral, freely withdrawable — borrowing/collateral is out of scope for #21) and `withdraw → Withdraw`. Validates the pool + reserve-asset contract ids (C…), the user account (G…), and a positive in-range i128 amount. Pure/offline, unit-tested (decoder round-trip → `submit` on the pool; from/spender/to = user; supply vs withdraw `request_type`; ascending map-key order; full i128 precision; bad pool/asset/account/amount rejections). **Remaining for #21:** wire this through the mini-app invoke bridge (typed `InvokeIntent`) + a supply/withdraw UI with a curated Blend testnet pool/asset directory, and a live Soroban RPC round-trip. (part of #21)
- 2026-07-07 — Blend: **curated testnet pool directory** (`core/blend/directory.ts`, part of #21). A small, vetted list (mirroring the anchor directory #54) of Blend lending pools + the reserve assets you can supply into them: `BlendPool`/`BlendReserve` types, `BLEND_POOLS`, `findBlendPool`, `blendPoolsForNetwork`, and `findReserve` (by asset code, case-insensitive). Unlike anchors (discovered from a stellar.toml), pools are addressed directly by contract id, so the ids are pinned here. Seeded with the Blend V2 testnet pool (`CCEBVDYM…`) supporting **USDC** and **XLM** (7 decimals each) — **validated live against `soroban-testnet.stellar.org`**: `get_reserve` simulates OK for USDC/XLM/wETH (BLND is not a reserve), and a `submit` supply built by `buildBlendSubmitXdr` (#67) is accepted and decoded by the pool contract (failing only downstream on the caller's token balance — proving the address + Request ScVal wire-format are correct). Testnet deployments are periodically reset, so the entry documents that it must be re-validated before it's relied on. Pure/offline data module, unit-tested (well-formed pool/reserve entries, valid C… contract ids, no duplicate reserve codes, verified testnet pool lists USDC+XLM, lookup hit/miss, network filter, case-insensitive reserve lookup). Next: a supply/withdraw UI wiring this directory to `buildBlendSubmitXdr` → simulate/assemble → scan → sign. (part of #21)
- 2026-07-07 — Blend: **pool `submit` supply/withdraw call-shape encoder** (`core/blend/pool.ts`, part of #21). The first Blend-specific surface on top of the now-complete Soroban invoke pipeline (`prepareInvoke` #65): `buildBlendSubmitXdr({ poolId, userAddress, reserveAssetId, amount, action, sourceSequence, networkPassphrase, fee? })` turns a "supply N of asset X to earn yield" / "withdraw N of asset X" intent into the **pre-simulation** `invokeHostFunction` XDR for Blend's pool `submit(from, spender, to, requests)` entrypoint, ready to feed `simulateTransaction` (#56) → `assembleInvokeXdr` (#64) → sign. **Why it can't reuse the scalar `InvokeArg` path (#61):** `submit` takes a `Vec<Request>` where each `Request` is a struct `{ address: Address, amount: i128, request_type: u32 }` — a **nested** ScVal (a vec of maps) the flat scalar arg schema can't express — so this module builds that ScVal shape directly, then hands off to the same simulate/assemble tail as every other invoke. The `Request` map keys are emitted in ascending order (`address` < `amount` < `request_type`) as the host requires; `from = spender = to = userAddress` (self-service wallet supply/withdraw, also the tx source). Exposes the full `BLEND_REQUEST_TYPE` enum (Supply=0 … DeleteLiquidationAuction=9, per blend-contracts-v2 `RequestType::from_u32`); the yield MVP maps `supply → Supply` (plain, non-collateral, freely withdrawable — borrowing/collateral is out of scope for #21) and `withdraw → Withdraw`. Validates the pool + reserve-asset contract ids (C…), the user account (G…), and a positive in-range i128 amount. Pure/offline, unit-tested (decoder round-trip → `submit` on the pool; from/spender/to = user; supply vs withdraw `request_type`; ascending map-key order; full i128 precision; bad pool/asset/account/amount rejections). **Remaining for #21:** wire this through the mini-app invoke bridge (typed `InvokeIntent`) + a supply/withdraw UI with a curated Blend testnet pool/asset directory, and a live Soroban RPC round-trip. (part of #21)
- 2026-07-04 — Stellar: **`prepareInvoke` — full build→simulate→assemble pipeline** (`core/stellar/invoke.ts` + `sorobanRpcUrl` on `NetworkConfig`, part of #21). Ties the Soroban invoke primitives into one call the mini-app bridge / a UI uses to turn a typed contract call into a **ready-to-sign XDR**: `prepareInvoke({ sourceAccount, sourceSequence, contractId, functionName, args, networkPassphrase, rpcUrl, fetchImpl?, inclusionFee? })` builds the pre-sim tx (#61) → simulates it against a Soroban RPC (#56) → assembles footprint + auth + resource fee (#64), returning `{ ok: true, xdr }` ready for the existing **scan → approve → SIGN_AND_SUBMIT** path. Every failure mode (bad arg, RPC/network error, contract would revert, missing footprint) comes back as `{ ok: false, error }` instead of throwing, so the caller surfaces one thing. Args are **typed** (`InvokeArg`) — the consumer resolves the intent's arg types. `NetworkConfig` gains an optional `sorobanRpcUrl` (set for Testnet = `soroban-testnet.stellar.org`; Mainnet left unset — no single canonical public RPC). Injectable `fetch`, unit-tested offline (happy path → decodes to the call + fee = inclusion+resource; contract-revert → `ok:false`; a bad arg returns `ok:false` **without** calling the RPC; a non-2xx RPC → `ok:false`). **Remaining for #21: the consumer** — extend the mini-app invoke bridge (typed `InvokeIntent`) or a UI to call `prepareInvoke` → scan → sign — plus a **Blend supply/withdraw surface** (needs real Blend pool contract IDs) and a **live Soroban RPC round-trip** to validate against real simulation output. (part of #21)
- 2026-07-04 — Anchor UI: **Cash in / Cash out — interactive transfer wired end-to-end** (`popup/screens/CashInOut.tsx` + new `popup/components/SandboxedFrame.tsx`, part of #24). Completes the on-ramp flow: picking a **Cash in**/**Cash out** direction now runs SEP-10 auth (`authenticateSep10` #59 — the challenge is validated against the anchor's SEP-1 `SIGNING_KEY`/domains **before** signing, and signed via `SIGN_ONLY` #33, never submitted), starts the SEP-24 interactive session (`startInteractive` #40), and hosts the anchor's hosted deposit/withdraw window in a new **bridge-less sandboxed iframe** (`SandboxedFrame`) with a live status strip driven by `pollTransferStatus` (#62) → `describeTransferStatus` (#55), settling on a terminal state with a **Done** action. `SandboxedFrame` reuses the mini-app Browser's sandboxed-iframe surface (opaque-origin sandbox + framing-blocked "open in new tab" fallback + safe-area overlay) **without** the wallet connect/sign postMessage bridge — a regulated anchor's page has no business talking to it. Completed deposits/withdrawals are real Horizon payments, so they surface in Activity + the balance on return with no extra wiring; and the only thing ever signed is the validated SEP-10 challenge (no anchor-driven arbitrary signing), satisfying "scanned/validated before signing". New pure `webAuthDomainFor()` (host of the WEB_AUTH_ENDPOINT, home-domain fallback), unit-tested. Screen verified via typecheck/lint/build; the end-to-end **Testnet demo against a live anchor** is the remaining acceptance item (needs a real anchor round-trip + KYC, not automatable here). (part of #24)
- 2026-07-04 — Stellar: **Soroban invoke `assemble` step + auth capture** (`assembleInvokeXdr` in `core/stellar/invoke.ts`, `auth` in `core/stellar/soroban.ts`, part of #21). Closes the gap between the invoke **builder** (#61) and signing: `simulateTransaction` (#56) now also surfaces the `auth` entries a call requires (from the simulation's `results[0].auth`), and `assembleInvokeXdr({ builtXdr, minResourceFee, transactionData, auth?, inclusionFee? })` applies a simulation result to a pre-simulation invoke tx to produce the **ready-to-sign** XDR: it attaches the simulated **footprint** (`SorobanTransactionData`) and **auth entries** to the single `invokeHostFunction` op and sets the total fee to `inclusion + minResourceFee` (a Soroban tx pays a classic inclusion fee **plus** the resource fee), preserving source/sequence/timebounds; rejects a fee-bump, a non-invoke or multi-op tx, and a missing footprint. Pure/offline. Unit-tested against SDK-constructed fixtures (footprint attached — tx ext arm 1; fee = 100+resource and a custom inclusion fee; call survives the rebuild; no-footprint throws) + simulate's auth capture (present / empty-omitted). **The build→simulate→assemble pipeline is now complete; wiring it to the signing path + a Blend supply/withdraw UI (needs real Blend pool contract IDs) is the next slice.** NOTE: the assemble inputs come from a live Soroban RPC — this is unit-tested against fixtures but only truly validated by a real RPC round-trip. (part of #21)
- 2026-07-04 — Anchor: **SEP-24 transaction status poller** (`core/anchor/poll.ts`, part of #24). The polling half of acceptance item 3: after `startInteractive` (#40) returns a transaction id, `pollTransferStatus()` polls `GET /transaction` (#40) until the deposit/withdraw reaches a terminal state, mapping each status through `describeTransferStatus` (#55). Pure and injectable — the `fetch` and the inter-poll `wait` are both injected — so it's fully unit-tested offline with no real timers. `onUpdate` fires only on an actual status *change* (so a UI subscription isn't spammed with identical `pending_anchor` ticks); terminal is decided by `describeTransferStatus(...).terminal`, so an unknown/future anchor status keeps polling rather than stopping early; a first-fetch failure throws while a later transient failure is swallowed and retried; `isCancelled()` stops the loop (e.g. screen unmount) and `maxAttempts` bounds it. Unit-tested (drive-to-terminal, change-only onUpdate, error-terminal, unknown-status keeps polling, first-fetch throw, transient-retry, cancel, maxAttempts). Wiring it into the Cash-in/out screen + folding completed transfers into Activity is the next slice. (part of #24)
- 2026-07-04 — Anchor: **SEP-10 auth → SEP-24 interactive orchestrator** (`core/anchor/session.ts`, part of #24). Ties the anchor transfer flow together as a pure, injectable orchestrator so the security-critical sequencing is unit-tested rather than buried in UI: `authenticateSep10()` fetches the SEP-10 challenge (#39), **validates it against the anchor's SEP-1 `SIGNING_KEY` + expected home/web-auth domains + network BEFORE signing**, and only a validated challenge is handed to the injected `signChallenge` (the app wraps `SIGN_ONLY` #33), then submits it for a JWT. `startAnchorTransfer()` chains that into `startInteractive` (#40), returning the interactive URL (for the `Browser` overlay) + tx id (to poll). The signer + `fetch` are injected, so it's fully offline-testable — including the guard tests that a challenge signed by the **wrong key** and one built for a **different network** are refused and the signer is **never called**. The Cash-in/out screen wiring (a `SIGN_ONLY`-backed signer + opening the interactive URL in the existing sandboxed `Browser` overlay) + status polling/Activity are the next slice. (part of #24)
- 2026-07-04 — Receive UI: **"Receive" address-QR screen** (`popup/screens/Receive.tsx` + pure `core/receive/payload.ts`, part of #19). New `qr_code_2` AppBar action opens a full-screen overlay (same pattern as Guardians/CashInOut, `receiveOpen` in `App.tsx`) that renders the wallet address as a scannable QR via the small well-known `qrcode` dep (`QRCode.toDataURL` → `<img>`), shows the full address in mono below it, and offers a "Copy address" button reusing the existing `useToast()` → "Address copied". The QR payload is factored into a pure `receiveQrPayload(address)` helper — raw account key is the receive standard; it validates via `StrKey.isValidEd25519PublicKey` and rejects non-keys/secrets so a secret can never be encoded — unit-tested (raw payload, whitespace-trim, invalid/secret/empty rejection). A SEP-0007 `web+stellar:pay` request URI is a deliberate follow-up. The camera-SCAN half (for Send) is out of scope here — it needs a native-camera-permission decision. Screen verified via typecheck/lint/build (repo has no component-test infra). (part of #19)
- 2026-07-04 — Stellar: **typed Soroban invoke-contract XDR builder** (`core/stellar/invoke.ts`, part of #21). The "invoke builder" the Blend integration needs — the construction counterpart to the merged `simulateTransaction` wrapper (#56), and the typed ScVal step the mini-app `InvokeIntent` (`core/miniapps/bridge.ts`) explicitly deferred (it carries args as bare strings). A typed-arg schema `InvokeArg = { type; value: string }` covers `address | symbol | string | bool | u32 | i32 | u64 | i64 | u128 | i128 | bytes`; `argToScVal(arg)` converts one arg via the SDK (`Address(...).toScVal()` for addresses accepting both G…/C…; `nativeToScVal` for 32-bit ints and symbols; `XdrLargeInt` for 64/128-bit ints at full precision; `scvString`/`scvBool`/`scvBytes` otherwise) and throws a clear Error on a bad value — non-numeric or out-of-range int (BigInt range-checked, since `nativeToScVal` silently wraps), invalid address, over-long/illegal symbol, odd-length or non-hex bytes. `buildInvokeContractXdr({ sourceAccount, sourceSequence, contractId, functionName, args, networkPassphrase, fee? })` validates the contract id (C…) and function name (Soroban symbol, ≤32) then builds a single-op `InvokeHostFunction` transaction via `new Contract(id).call(fn, ...scVals)` + `setTimeout` and returns `.build().toXDR()`. **Scope: this is the PRE-simulation tx** — resource fees / footprint come from `simulateTransaction` (#56); applying that estimate (assemble) + wiring to signing is a follow-up. Pure builder, no network, no new deps. Unit-tested: build→`decodeTransaction` round-trip (contractId + function match), per-type `scValToNative(argToScVal(...))` value recovery, and rejection of bad contract id / function name / typed values. (part of #21)- 2026-07-04 — Stellar: **Soroban RPC `simulateTransaction` wrapper** (`core/stellar/soroban.ts`, part of #21). The "RPC-simulated invoke" primitive the Blend integration needs — the simulation half deferred from the invoke-intent work (#36). `simulateTransaction(txXdr, { rpcUrl, fetchImpl? })` POSTs a JSON-RPC 2.0 `simulateTransaction` to a Soroban RPC node and normalizes the reply into a discriminated union: success → `{ ok: true, minResourceFee, transactionData?, latestLedger? }`; the contract would revert (`result.error` set) or a JSON-RPC top-level `error` → `{ ok: false, error }`; a non-2xx HTTP response throws with the status (mirrors the SEP clients); a malformed/unreadable body degrades to `{ ok: false, error: 'Unreadable simulation response.' }`. The INPUT is an already-built transaction envelope XDR — this module builds no ScVals or invoke op. The transport is an injectable `fetch` (not `rpc.Server`), so the whole path is unit-tested offline (success parse, contract-failure, JSON-RPC error, non-2xx throw, malformed body). Ships a default `SOROBAN_TESTNET_RPC` constant while the function still takes `rpcUrl` explicitly. Feeds the eventual RPC-simulated invoke builder. (part of #21)
- 2026-07-04 — Scan: **plain-language labels for DeFi contract calls** (`core/scan/defi.ts` + the Soroban branch of `core/scan/explainer.ts`, part of #21). Builds on decode exposing `contractFunction`/`contractId` (#34) and the plain-language setOptions work (#37) — the same "spell it out before signing" goal. A curated map turns well-known DeFi/lending function names into a short human phrase (`supply`/`deposit` → "Deposits funds into a lending pool", `withdraw` → "Withdraws funds from a lending pool", `borrow` → "Borrows against your collateral", `repay` → "Repays borrowed funds", `claim`/`claim_rewards` → "Claims rewards"), matched case-insensitively on the decoded Soroban symbol. When recognized, the approval sentence leads with the action and still names the raw function + contract (e.g. `Deposits funds into a lending pool — calls "supply" on a smart contract (CA3D…GAXE). Only continue if you trust it.`); unrecognized functions and non-invoke Soroban ops (upload-wasm / create-contract) keep the existing generic wording. Pure, wording-only — no new deps, no network, no change to decode/types or scan verdicts. Unit-tested (recognized phrase + still names the fn, case-insensitivity, unrecognized regression guard, no-function fallback). (part of #21)
- 2026-07-04 — Anchor UI: **"Cash in / Cash out" anchor-discovery screen** (`popup/screens/CashInOut.tsx` + `core/anchor/transfer.ts`, part of #24 item 4). First user-facing surface for the fiat ramp — a new `currency_exchange` AppBar action opens a full-screen overlay (same pattern as Guardians/Scan) that lists the curated anchors for the current network (`anchorsForNetwork` #54), and on selecting one runs SEP-1 discovery (`discoverAnchor` #38) → SEP-24 `GET /info` (`fetchSep24Info` #52) to show which assets you can **cash in** (deposit) or **cash out** (withdraw), with per-asset limits. A new pure `summarizeAssetSupport()` merges the `/info` deposit+withdraw maps into one row per asset (a side counts only if listed **and** enabled; assets with nothing usable are dropped) and `formatTransferLimits()` renders min/max — both unit-tested. Picking a direction advances to a handoff step describing the next step (sign a one-time SEP-10 auth challenge — never submitted — then complete in the anchor's secure window, with any signature scanned first); the actual SEP-10 auth + SEP-24 interactive (in the existing `Browser` overlay) + status polling / Activity integration are the next slice. Screen verified via typecheck/lint/build; not yet runtime-verified in-browser (needs the Testnet anchor round-trip). (part of #24)
- 2026-07-04 — Anchor: **SEP-24 `/info` asset discovery** (`fetchSep24Info` in `core/anchor/sep24.ts`, part of #24). Completes the SEP-24 client (alongside `startInteractive` + `fetchTransaction` #40): `GET /info` on the anchor's `TRANSFER_SERVER_SEP0024` returns which assets support deposit/withdraw plus per-asset limits (`min_amount`/`max_amount`) and fees (`fee_fixed`/`fee_percent`), so a "Cash in / Cash out" screen can list the supported assets (e.g. XLM/`native`, USDC) **before** the user authenticates — `/info` is a public endpoint, so no SEP-10 JWT is attached. Assets are returned sorted by code with `enabled` preserved (SEP-24's omit-means-true honored; only an explicit `false` disables) so the UI can grey out disabled ones; numeric fields are defensively coerced and a response missing `deposit`/`withdraw` degrades to empty lists. Injectable `fetch`, unit-tested offline (maps limits/fees, enabled defaulting, trailing-slash + missing-sides tolerance, non-2xx throws). Satisfies the #24 "SEP-24 `/info`" acceptance item; the anchor directory + Cash-in/out UI remain. (part of #24)
- 2026-07-04 — Anchor: **curated anchor directory** (`core/anchor/directory.ts`, part of #24 item 4). A small, vetted list of anchors for the Cash in / Cash out demo — verified/vetted entries, not an open free-for-all — mirroring the existing curated `core/miniapps/directory.ts` (`AnchorEntry`/`ANCHORS`/`findAnchor`, plus `anchorsForNetwork` filtering and an `isValidHomeDomain` guard). Seeded with the SDF reference SEP-24 testnet anchor (`testanchor.stellar.org`, SRT + USDC). Pure/offline: entries carry the SEP-1 `homeDomain` that `discoverAnchor` fetches `.well-known/stellar.toml` from — no network calls here. Unit-tested (seed well-formed, lookup hit/miss, network filter, domain guard). (part of #24)
- 2026-07-04 — Anchor: pure **SEP-24 transfer-status labels** (`src/core/anchor/status.ts`) — `describeTransferStatus` maps each `GET /transaction` status to a plain-language label, a `kind` (pending / action-needed / done / error), and a `terminal` flag, with a safe fallback for unknown anchor values. Feeds the upcoming status-polling + Activity surfacing. (part of #24)
- 2026-07-04 — Recovery UI: **recover-an-account flow** (`RecoverAccount.tsx`, part of #23 M1 item 4 — completes the recovery flow end-to-end). The recovering device's side: from a "Lost access to another account? Recover it" link on the Guardians screen, enter the account you lost, and it reads that account's guardians (`loadAccountSigners` + `classifyGuardianConfig` #45), builds the recovery request that adds **this** device's key (`buildRecoveryXdr` #42), shows it to share with the guardians, collects their pasted-back signed copies (`mergeGuardianSignatures` #47 — mismatched copies rejected), tracks weight collected vs the account's threshold (`collectedSignatureWeight` #43), and once enough are gathered broadcasts the merged tx with `SUBMIT_ONLY` (#50 — no self-signature, so no `tx_bad_auth_extra`). Thin orchestrator over already-tested primitives. (#51, part of #23)
- 2026-07-04 — Session: **`SUBMIT_ONLY` broadcast-an-already-signed-tx primitive** (`core/session/handler.ts`, part of #23 M1 item 4). New worker message that submits a fully-signed transaction to Horizon **without adding a signature**. The recovering device needs this to broadcast the guardian-signed recovery tx: its own key is **not** a signer on the account being recovered, so `SIGN_AND_SUBMIT` would attach an unused signature and Horizon would reject the whole tx with `tx_bad_auth_extra`. Unlike the signing paths it needs **no unlocked session** (it only broadcasts, never touches the key). Reachable on extension + native via the generic worker forward; unit-tested (not session-gated; malformed XDR fails gracefully, never `LOCKED`). Unblocks the recovering-device submit step. (#50, part of #23)
- 2026-07-04 — Recovery UI: **guardian co-sign screen** (`CoSignRecovery.tsx`, part of #23 M1 item 4). The guardian side of social recovery: reached from a "Helping someone recover? Co-sign their request" link on the Guardians screen, you paste the recovery request someone shared, review exactly what it changes (existing decode/scan/explain #31/#37 → `RiskCallout` → CONFIRM/press-and-hold gate), co-sign it with `SIGN_ONLY` (#33 — signed, never submitted here), and copy the signed request back. **Safe by construction:** a new pure `recoveryCoSignError()` guard refuses to sign unless the request (a) is readable, (b) does **not** modify the guardian's *own* account — a self-sourced tx co-signed with the guardian's master key would be an account **takeover** of the guardian, so `source === ownAddress` is rejected — and (c) is a pure recovery (every op a setOptions signer/threshold change, via `isRecoveryTransaction`), so a payment (or a mixed tx) can't be dressed up as a "recovery." It's not a generic sign-any-XDR oracle. Guard unit-tested incl. the takeover case (accepts an other-account recovery; rejects a self-sourced setOptions, a payment, and unreadable input). (#49, part of #23)
- 2026-07-04 — Recovery UI: **manage guardians (edit / add / remove)** on the Guardians screen (`Guardians.tsx`, part of #23 M1). Lifts the re-setup block from #45 by routing edits through the safe `buildGuardianUpdateXdr` (#46). When an account already has recovery, the screen becomes an editor: it prefills the guardian list + threshold from the account's current signers, and on submit builds an *update* tx (weight-0 removals + adds + thresholds recomputed from the final set — anti-drain invariant preserved) instead of a first-time setup. The review screen shows an added/removed diff (new pure `guardianDiff()` helper, unit-tested) and routes through the same high-risk scan → confirm gate; copy adapts ("Manage Guardians" / "Update Recovery"). First-time setup on a fresh account is unchanged. (#48, part of #23)
- 2026-07-03 — Recovery: **`mergeGuardianSignatures` — combine out-of-band co-signatures** (`core/recovery/guardians.ts`, part of #23 M1 item 4). The recovery-coordination flow shares one recovery XDR to each guardian, who signs a copy on their own device via `SIGN_ONLY` (#33); this merges all their signatures back onto the base tx ready to submit. Security-critical guard: every signed copy must be the **same transaction** (matching tx hash) as the base — a copy for a *different* tx is rejected, so a malicious guardian can't get their signature counted toward a transaction they swapped in. Duplicate signatures are ignored. Pure/offline; composes with the co-signature tally (#43) to know when enough weight is collected. Unit-tested (combine two guardians, dedup, reject-different-tx, empty). (#47, part of #23)
- 2026-07-03 — Recovery: **`buildGuardianUpdateXdr` — safe add/replace/remove of guardians** (`core/recovery/guardians.ts`, part of #23 M1). The setup builder only *adds* signers, so it can't safely change an existing guardian set (#45 blocks re-setup for that reason). This new builder transitions `currentGuardians → desiredGuardians`: emits weight-0 **removals** for dropped guardians, adds new ones, and recomputes the owner weight + thresholds from the **final** set (`ownerWeight = desired+1`, `high = K`) — so the anti-drain invariant ("all guardians combined < the medium threshold") holds *after* the change, not just at first setup. Precondition: caller supplies the account's complete on-chain guardian set (from `classifyGuardianConfig`). Shared guardian validation refactored out of the setup builder. Unit-tested incl. the invariant (grow to 4 → medium threshold 5 > 4). Wiring this into a manage-guardians UI (to unblock #45's re-setup) is the follow-up. (#46, part of #23)
- 2026-07-03 — Recovery UI: **current-guardians view + block unsafe re-setup** on the Set Up Recovery screen (`Guardians.tsx`, part of #23 M1). The screen didn't show an account's existing guardians, and — because `buildGuardianSetupXdr` only *adds* signers (setOptions never removes the others) while recomputing thresholds for the newly-entered count — re-running setup on a configured account would **accumulate** guardians and silently break the anti-drain threshold invariant. Now, on open, it reads the account's signers via a new `loadAccountSigners()` and a pure `classifyGuardianConfig()` (guardians = non-master signers; recovery threshold = the account's high threshold); if recovery is already on it shows a read-only "Recovery is on — any K of N can recover" card listing the guardians and **blocks re-setup** (form + Review hidden, `toReview` guarded) with an accurate "changing guardians isn't supported yet" note, since the builder is first-time-setup-only. Classifier unit-tested. (#45, part of #23)
- 2026-07-03 — Recovery UI: **"Set Up Recovery" guardian screen** (`src/popup/screens/Guardians.tsx`, #23 M1 item 3). First user-facing surface for guardian social recovery — reached from a new `shield_person` AppBar action, opened as a full-screen overlay (same pattern as the Security/Scan screen). Add guardian addresses, pick a K-of-N recovery threshold, then it builds the setup tx via `buildGuardianSetupXdr` (#41), runs it through the existing Lantern pre-sign **scan → RiskCallout → CONFIRM/press-and-hold gate** (high-risk account-control change, #31/#37), signs & submits, and shows a success + explorer link. Mirrors the Send flow; reuses `buildGuardianSetupXdr`'s validation for guardian keys/threshold. New pure `describeGuardianSetup()` renders the plain-language quorum summary ("Any 2 of your 3 guardians can help you recover…"), unit-tested. The review screen shows the **true total** network fee via a new `totalFeeXlm()` helper — the setup is a multi-op tx so the fee is `baseFee × (N+1)`, not `baseFee` — regression-tested. (#44, part of #23)
- 2026-07-03 — Recovery: **guardian co-signature tally** (`collectedSignatureWeight` / `hasThresholdSignatures` in `core/recovery/guardians.ts`). The last non-UI piece of the recovery coordination flow (#23 M1, item 4): as guardians co-sign a recovery tx out-of-band via `SIGN_ONLY` (#33), these compute how much signature weight has been collected (via the SDK's `WebAuth.gatherTxSigners`, summing only the account's known signers) and whether it has reached the account's high threshold K — i.e. when it's safe to submit. Only listed signers count, so a stray signature can't inflate the tally. Pure/offline; unit-tested (0/1/2-of-3 tallies, per-signer weights, unknown-signature rejection, threshold reached at K). (#43, part of #23)
- 2026-07-03 — Recovery: **`buildRecoveryXdr` — install-new-key recovery transaction** (`core/recovery/guardians.ts`). Completes the transaction-building side of guardian recovery (#23 M1, item 4): when the owner key is lost, the fresh device drafts one `setOptions` that installs its new key at owner-level weight (`N+1`, mirroring the setup so it operates normally) and disables the lost master (`masterWeight 0`). It's a HIGH-threshold change, so under the `highThreshold = K` the setup installed it's authorized by exactly K guardian co-signatures (each collected out-of-band via `SIGN_ONLY` #33). Thresholds + existing guardian signers are left untouched, so the account stays guardian-protected. Validates the new key, rejects self. Also tightens `buildGuardianSetupXdr`'s guardian cap from 20 → **19** (`MAX_SIGNERS − 1`) so a recovery signer slot is always reserved — otherwise an `N=20` setup would produce a permanently unrecoverable account within Stellar's 20-signer limit. Unit-tested via the scan decoder (#31) + confirmed scanned high-risk and explained as *"adds signer … removes your own key's signing power"* (#31/#37), plus the 19-guardian headroom boundary. The Guardians & Recovery UI + QR/paste co-sign hand-off remain the rest of #23. (#42, part of #23)
- 2026-07-03 — Recovery: **`buildGuardianSetupXdr` guardian multi-sig setup** (`core/recovery/guardians.ts`). Composes the setOptions primitive (#35) into the actual weighted-multisig setup transaction for guardian social recovery (#23 M1, items 3–4): adds each guardian as a weight-1 signer (one op each), then sets the account's weighting. Security-critical policy (decouples owner power from the guardian quorum, since Stellar puts **Payment on the *medium* threshold but signer `setOptions` on the *high* threshold**): owner `masterWeight` + `low`/`medThreshold` = `N+1` (strictly above all guardians combined → guardians can **never** authorize a payment/offer/trustline change), and **only** `highThreshold` = `K` so any K guardians can co-sign the recovery `setOptions` and nothing lower. Owner keeps full solo control, never locked out. Validates distinct valid keys, `1 ≤ K ≤ N ≤ 20`, rejects self-as-guardian. Pure/offline; unit-tested by round-tripping through the scan decoder (#31), asserting the on-chain invariant (K guardians clear high but not medium), and confirming the scanner flags it high-risk `block_confirm` (#31/#37). The Guardians & Recovery UI + QR/paste co-sign hand-off remain the rest of #23. (#41, part of #23)
- 2026-07-03 — Anchor: **SEP-24 interactive transfer client** (`core/anchor/sep24.ts`). The transfer step after SEP-10 auth (#24): `startInteractive(transferServer, {kind, assetCode, account, jwt})` POSTs a deposit/withdraw to `TRANSFER_SERVER_SEP0024/transactions/{deposit|withdraw}/interactive` (bearing the SEP-10 JWT) and returns the interactive `url` (to open in the mini-app Browser overlay) + transaction `id`; `fetchTransaction` polls `/transaction?id=` and maps status/amounts. Injectable `fetch`, unit-tested offline. (#40, part of #24)
- 2026-07-03 — Anchor: **SEP-10 web authentication client** (`core/anchor/sep10.ts`). The auth step between SEP-1 discovery (#38) and any SEP-24 transfer (#24): `fetchChallenge` GETs the anchor's challenge from `WEB_AUTH_ENDPOINT`; `validateChallenge` wraps the SDK's `WebAuth.readChallengeTx` to verify the challenge is server-signed by the SEP-1 `SIGNING_KEY`, sequence 0, in-timebounds, and for the right home/web-auth domain — the guard that makes handing an anchor transaction to `SIGN_ONLY` (#33) safe; `submitChallenge` POSTs the signed challenge back for the SEP-10 JWT. Network calls take an injectable `fetch`, so the whole flow (incl. real challenge fixtures via `WebAuth.buildChallengeTx`) is unit-tested offline. (#39, part of #24)
- 2026-07-03 — Anchor: **SEP-1 `stellar.toml` discovery** (`core/anchor/toml.ts`). First step of the "Cash in / Cash out" flow (#24): `parseAnchorToml(text)` extracts the SEP-10 `WEB_AUTH_ENDPOINT` + `SIGNING_KEY` (validated as a real account key, dropped if not) and the SEP-24 `TRANSFER_SERVER_SEP0024`, plus `[[CURRENCIES]]`. `discoverAnchor(homeDomain, fetchImpl?)` fetches `https://<domain>/.well-known/stellar.toml` (scheme/trailing-slash tolerant) and parses it; `fetchImpl` is injectable so the whole path is unit-tested offline. Adds `toml@3.0.0` (MIT) as a **new** production dependency — the SDK exposes only a network `StellarToml.Resolver` (no pure string parse), and since the 15→16 bump (#32) it no longer bundles a standalone `toml` parser (it moved to `smol-toml` internally, not re-exported) — so we pull in a small, well-known MIT parser to keep SEP-1 discovery pure and offline-testable. (#38, part of #24)
- 2026-07-03 — Scan: **spell out signer/threshold changes in plain language**. The `setOptions` explanation was a single generic sentence; it now names exactly what changes — *"This adds signer GDVE…ZA57 with weight 1, and sets the signing thresholds (low 1, medium 2, high 2)"*, *"removes signer …"* (weight 0), *"removes your own key's signing power"* (masterWeight 0) — from the fields already decoded in #31. Directly satisfies the #23 acceptance criterion ("explains signer/threshold changes in plain language"); the highest-stakes op type to get right, since a signer change is how an account gets taken over. Pure, unit-tested. (#37, part of #23)
- 2026-07-03 — Mini-app bridge: **contract-invocation intent shape**. `core/miniapps/bridge.ts` could only model a classic `PaymentIntent`; add `validateInvokeIntent` (+ `InvokeIntent` type and a `isValidContractId` helper in `wallet/`) so a DeFi mini-app (e.g. Blend `supply`) can request a Soroban contract call through the same connect → scan → approve → sign pipeline as payments. Validates the `C…` contract address, the function name (Soroban symbol: alnum/underscore, ≤32), and normalizes positional args to strings (typed ScVal construction is deferred to the RPC-simulated invoke builder). Pure, unit-tested; pairs with the contract-call scan decode (#34). (#36, part of #21)
- 2026-07-03 — Stellar: **`buildSetOptionsXdr` weighted-multisig builder**. New pure tx builder for the social-recovery primitive (#23): add/remove a guardian signer (weight 0 removes) and/or set low/med/high thresholds + `masterWeight`. Emits only the fields actually being changed (an unspecified threshold is never silently zeroed), validates weights 0–255, and throws on an empty (no-op) change. Completes the #23 M1 primitive set alongside the setOptions scan-decode (#31) and the `SIGN_ONLY` co-signing path (#33); unit-tested by round-tripping the built XDR back through the scan decoder. (#35, part of #23)
- 2026-07-02 — Scan: **name the Soroban contract call**. `decode.ts` previously only set an `isSoroban` boolean, so every contract invocation scanned as a generic "Smart contract call". Now it decodes the `invokeHostFunction` host function into `contractId` + `contractFunction`, and the scan reason + explainer name what's being called (e.g. *"This calls 'supply' on contract CA3D…GAXE"*). Upload-wasm / create-contract host functions (no callable fn) still decode safely and stay flagged as Soroban. Pure decode, defensive parsing, unit-tested; the "Soroban contract scanning" groundwork the Blend integration needs. (#34, part of #21)
- 2026-07-02 — Session: **`SIGN_ONLY` sign-but-don't-submit primitive**. New worker message that signs a transaction with the in-memory keypair and returns the signed XDR **without** submitting to Horizon (mirrors `SIGN_AND_SUBMIT`, stops before broadcast). Because `fromXDR` preserves existing signatures and `sign()` appends, it doubles as a **co-signing** primitive — a guardian adds their signature to a partially-signed recovery tx, and SEP-10 signs a challenge tx that's returned, never submitted. Locked-session gated; unit-tested (signature verifies against the account, and a second call appends a 2nd signature). Unblocks anchor SEP-10 auth (#24). (#33, part of #23)
- 2026-07-02 — Security/deps: **`@stellar/stellar-sdk` 15.1.0 → 16.0.1** to clear the transitive `axios` advisories (bumps `axios` 1.15.x → 1.16.1, above the vulnerable range — prototype-pollution/credential-leak class). `npm audit --omit=dev` now reports 0 production vulnerabilities. Regression pass on the SDK surface (tx builders, Horizon client, session handler, scan decode): typecheck, lint, all 75 unit tests, and the production build pass unchanged — no source changes needed. (The one remaining dev-only `vite` advisory is not shipped and is left out of scope.) (#32, part of #18)
- 2026-07-02 — Scan: **flag signer/threshold changes before signing**. The scanner now decodes `setOptions` (signers, `masterWeight`, low/med/high thresholds) instead of letting it fall through as an unrecognized op, and raises a high-risk `account_control_change` verdict (`block_confirm`) — with a stronger "gives up account control" message when `masterWeight` is set to 0. Previously a signer/threshold change scanned as low-risk `allow`. Pure decode + engine rule, unit-tested; first consumer of the account-control decode groundwork for guardian recovery. (#31, part of #23)
- 2026-07-02 — Native: **live settings propagation**. `onSettingsChanged` was a no-op on native, leaving non-active surfaces stale after e.g. a network toggle; add an in-process subscriber set that `setSettings` fans out to on native (matching the extension's `chrome.storage.onChanged` behavior), unit-tested. (#30, part of #19)
- 2026-07-02 — Mobile: **press-and-hold to confirm high-risk signs**. Replaces the typed-`CONFIRM` gate on native (Send + mini-app Apps approval) with a reusable `HoldToConfirm` button that fills a progress track and fires once — no on-screen keyboard, same "no accidental tap" guarantee. Extension keeps typed-`CONFIRM`. (#29, part of #19)
- 2026-07-02 — UX: **Send recent-recipients shortcut**. Pure `recentRecipients()` derives distinct, newest-first sent-to addresses from decoded tx history (outgoing payments + account creations; ignores incoming/swap/failed/self), unit-tested; Send form shows up to 3 as tappable chips that refill the destination. (#28, part of #19)
- 2026-07-02 — Android launcher icon: replace Capacitor's placeholder with the real **Lantern amber mark** across all mipmap densities (legacy square, circle-masked round, and adaptive foreground) + navy adaptive background. Generated from the same source art as the extension icons (`logo.jpg`) via a new cross-platform `scripts/gen-android-icons.mjs` (`npm run icons:android`, sharp-based) so the two sets can't silently diverge. (#25, closes #20)
- 2026-07-02 — Security: **Android lifecycle auto-lock**. Backgrounding the app (home/app-switcher) previously left the in-memory session unlocked for the full idle window; add a native `@capacitor/app` `pause` listener (gated on `isNativePlatform`, dynamically imported) that locks immediately on background, complementing the idle timer. (#27, part of #18)
- 2026-07-02 — UX: **copy-address confirmation toast**. Copying the wallet address (AppBar) or the demo-flagged address (Scan) was a silent clipboard write; add a minimal app-wide `ToastProvider`/`useToast` (aria-live) showing "Address copied" on success. (#26, part of #19)
- 2026-07-01 — Android fix: mini-app **browser overlay clears the status bar & gesture bar**. `#root`'s `transform` makes it the containing block for the `fixed` overlay, but a transformed ancestor insets against its *padding box* — so `inset-0` overlapped `#root`'s safe-area padding and ran the header under the status bar. Inset the overlay box itself via `env(safe-area-inset-*)`; extension/desktop unaffected (env → 0). (closes #9)
- 2026-06-30 — Mini-app bridge v2: issued (non-native) **asset payments** + read-only **sign-message** auth (`lantern:signMessage` → domain-separated `SIGN_MESSAGE`), with the intent logic extracted to a unit-tested `core/miniapps/bridge.ts`. Mock dApp gains an asset selector + Sign-message panel. (#13)
- 2026-06-30 — Mini-app `signAndSubmit` bridge: a connected dApp sends a payment **intent**; Lantern builds, AI-scans, has the user approve, signs & submits, returns the tx hash. Richer Get Balance mock dApp (send, all assets, Friendbot fund). (#12)
- 2026-06-30 — Read-only wallet **connect bridge** (`lantern:getPublicKey` → approval → public key + network) + deployable Get Balance mock dApp. (#11, closes #10)
- 2026-06-30 — Android layout: fill the device viewport (`100dvh`) + safe-area insets + status-bar overlay so the UI isn't a fixed 360×600 box. (#8, closes #7)
- 2026-06-30 — Android (Capacitor) milestone 1: storage/messaging seams, in-process session handler, mobile build, committed `android/` project, debug-APK CI. (#6)
- 2026-06-24 — Demo: mock mini-app browser launching blockhub.academy + AI scan on send; investor pitch deck and screenshots. (#3, closes #2)

<!-- auto-dev: prepend new entries above this line -->
